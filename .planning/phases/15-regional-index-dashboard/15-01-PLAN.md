---
phase: 15-regional-index-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants/region-codes.ts
  - src/lib/cluster-index.ts
  - src/components/charts/ClusterIndexChart.tsx
  - src/app/index/page.tsx
  - src/app/index/[clusterId]/page.tsx
  - src/components/layout/MobileNav.tsx
autonomous: true
requirements: [INDEX-01, INDEX-02, INDEX-04]
must_haves:
  truths:
    - "CLUSTER_DEFINITIONS 상수에 강남3구, 마용성, 노도강, 수도권 주요 4개 군집이 정의되어 있다"
    - "computeClusterIndex가 regionCodes 배열을 받아 월별 중위가 지수(기준시점=100)를 반환한다"
    - "/index 페이지에 군집별 카드 그리드가 표시된다 (현재 지수 + 변동 + 스파크라인)"
    - "/index/[clusterId] 페이지에 해당 군집의 전체 시계열 LineChart가 표시된다"
    - "시계열 차트에 기준선 100이 점선으로 표시된다"
    - "MobileNav에 지역 지수 링크가 존재한다"
  artifacts:
    - path: "src/lib/constants/region-codes.ts"
      provides: "ClusterDefinition interface + CLUSTER_DEFINITIONS 상수"
      contains: "CLUSTER_DEFINITIONS"
    - path: "src/lib/cluster-index.ts"
      provides: "computeClusterIndex 함수"
      exports: ["computeClusterIndex", "ClusterIndexPoint"]
    - path: "src/components/charts/ClusterIndexChart.tsx"
      provides: "Recharts LineChart + ReferenceLine 지수 차트"
      contains: "use client"
    - path: "src/app/index/page.tsx"
      provides: "군집 대시보드 페이지"
      contains: "revalidate"
    - path: "src/app/index/[clusterId]/page.tsx"
      provides: "군집 상세 시계열 페이지"
      contains: "revalidate"
  key_links:
    - from: "src/app/index/page.tsx"
      to: "src/lib/cluster-index.ts"
      via: "computeClusterIndex 호출"
      pattern: "computeClusterIndex"
    - from: "src/lib/cluster-index.ts"
      to: "src/lib/price-normalization.ts"
      via: "computeMedianPrice, groupByMonth import"
      pattern: "import.*price-normalization"
    - from: "src/app/index/[clusterId]/page.tsx"
      to: "src/components/charts/ClusterIndexChart.tsx"
      via: "chart 렌더링"
      pattern: "ClusterIndexChart"
---

<objective>
군집 지수 엔진과 S&P500 스타일 대시보드 페이지를 생성한다.

Purpose: 사용자가 강남3구/마용성/노도강/수도권 주요 군집의 가격 흐름을 한눈에 파악할 수 있게 한다.
Output: /index 대시보드 + /index/[clusterId] 상세 페이지 + 지수 계산 유틸
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-regional-index-dashboard/15-RESEARCH.md

@src/lib/price-normalization.ts
@src/lib/constants/region-codes.ts
@src/lib/db/client.ts
@src/components/charts/MiniAreaChart.tsx
@src/components/charts/MiniAreaChartWrapper.tsx
@src/components/layout/MobileNav.tsx
@src/app/market/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/price-normalization.ts:
```typescript
export function computeMedianPrice(prices: number[]): number;
export function groupByMonth(txns: { trade_date: string; trade_price: number }[]): { month: string; prices: number[] }[];
export function filterTransactions<T extends { floor: number; trade_price: number; deal_type: string | null }>(
  txns: T[],
  opts: { lowFloorMode: 'adjust' | 'include' | 'exclude'; recentMedian: number }
): { normal: (T & { original_price?: number })[]; directDeals: T[]; excluded: T[] };
export function isDirectDeal(dealType: string | null): boolean;
```

From src/lib/db/client.ts:
```typescript
export function getPool(): Pool;
```

From src/lib/constants/region-codes.ts:
```typescript
export interface Sido { name: string; shortName: string; slug: string; sigungu: Record<string, string>; }
export const REGION_HIERARCHY: Record<string, Sido>;
```

From src/components/charts/MiniAreaChartWrapper.tsx:
```typescript
export default function MiniAreaChartWrapper(props: {
  data: { value: number }[];
  color?: string;
  height?: number;
  label?: string;
}): JSX.Element;
```

MobileNav NAV_ITEMS array (line 7-21): Array of { href, label } objects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cluster definitions + index engine + chart component</name>
  <files>
    src/lib/constants/region-codes.ts,
    src/lib/cluster-index.ts,
    src/components/charts/ClusterIndexChart.tsx
  </files>
  <read_first>
    src/lib/constants/region-codes.ts,
    src/lib/price-normalization.ts,
    src/lib/db/client.ts,
    src/components/charts/MiniAreaChart.tsx
  </read_first>
  <action>
    1. **region-codes.ts** (per D-01, D-02, D-03): Add at bottom of file:
       - `ClusterDefinition` interface: `{ id: string; name: string; regionCodes: string[] }`
       - `CLUSTER_DEFINITIONS` array with 4 clusters:
         - gangnam3: 강남3구 ["11680", "11650", "11710"]
         - mayongseong: 마용성 ["11440", "11170", "11200"]
         - nodogang: 노도강 ["11350", "11320", "11305"]
         - sudobukmain: 수도권 주요 ["41135", "41465", "41285", "41287"]
       - NOTE: 수지구=41465 (NOT 41463 from CONTEXT D-02 which was a typo, per RESEARCH pitfall 1)

    2. **cluster-index.ts** (per D-04, D-05, D-06, D-07, D-08): Create new file:
       - Export `ClusterIndexPoint` interface: `{ month: string; index: number; medianPrice: number; count: number }`
       - Export `computeClusterIndex(regionCodes: string[], minTransactions = 3): Promise<ClusterIndexPoint[]>`
       - Use `getPool().query()` with parameterized SQL: SELECT trade_date, trade_price, floor, deal_type FROM apt_transactions WHERE region_code IN ($1,$2...) AND property_type = 1 ORDER BY trade_date ASC
       - Filter: use `isDirectDeal()` to exclude direct deals (per RESEARCH open question 1: pass recentMedian=0 to filterTransactions so 90% threshold is disabled, OR simply filter out direct deals manually since we don't need the full filterTransactions categorization)
       - Simpler approach: just filter out rows where `isDirectDeal(deal_type) === true` before grouping. This is sufficient for cluster-level index (we don't need adjust/exclude logic for floors at cluster level).
       - Group remaining with `groupByMonth(filtered)`
       - Find baseMonth: first month with `prices.length >= minTransactions` (per RESEARCH pitfall 2)
       - Compute baseMedian via `computeMedianPrice(baseMonth.prices)`
       - Map each qualifying month to `{ month, index: round((median/baseMedian)*1000)/10, medianPrice: median, count }`

    3. **ClusterIndexChart.tsx** (per D-14, D-15, D-16, D-18): Create "use client" component:
       - Props: `{ data: { month: string; index: number; count: number }[] }`
       - Recharts: ResponsiveContainer (100% width, 300px height) > LineChart > XAxis (dataKey="month", interval="preserveStartEnd", fontSize 11) > YAxis (domain auto, width 50) > Tooltip > ReferenceLine (y=100, stroke="#9CA3AF", strokeDasharray="4 4", label="기준(100)") > Line (dataKey="index", stroke="#2B579A", strokeWidth 2, dot=false, isAnimationActive=false)
       - Do NOT use dynamic import for this component — "use client" directive is sufficient since it will be imported by a server component that passes data as props.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ClusterDefinition and CLUSTER_DEFINITIONS exported from region-codes.ts with 4 clusters
    - computeClusterIndex returns ClusterIndexPoint[] with month/index/medianPrice/count
    - ClusterIndexChart renders LineChart with ReferenceLine at 100
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>3 files created/modified, tsc --noEmit passes</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page + detail page + nav link</name>
  <files>
    src/app/index/page.tsx,
    src/app/index/[clusterId]/page.tsx,
    src/components/layout/MobileNav.tsx
  </files>
  <read_first>
    src/app/market/page.tsx,
    src/components/layout/MobileNav.tsx,
    src/components/charts/MiniAreaChartWrapper.tsx
  </read_first>
  <action>
    1. **src/app/index/page.tsx** (per D-09, D-10, D-11, D-12): Server component:
       - `export const revalidate = 3600;` (ISR, per RESEARCH pitfall 4)
       - Metadata: title "지역 지수 대시보드 - 군집별 아파트 가격 지수", description, canonical "/index"
       - Import CLUSTER_DEFINITIONS, computeClusterIndex, MiniAreaChartWrapper, Link, formatPrice
       - Data fetching: `Promise.all(CLUSTER_DEFINITIONS.map(async cluster => { ... }))` — for each cluster call computeClusterIndex, extract latest/prev index, compute change, build sparklineData
       - Layout: max-w-6xl centered, heading "지역 지수 대시보드", subtext
       - Card grid (per D-10): `grid gap-3 sm:grid-cols-2 lg:grid-cols-3`
       - Each card (per D-11): Link to `/index/${cluster.id}`, shows cluster.name, currentIndex (1 decimal), change with arrow and color (green>=0, red<0), MiniAreaChartWrapper with sparklineData
       - IMPORTANT: MiniAreaChart linearGradient id uses color prop — if multiple cards share same color (e.g. all green), ids collide. Workaround: pass unique color per card by slightly varying hex, OR accept the limitation since gradient looks identical anyway (recommend accepting — visual difference is negligible).
       - Handle empty data gracefully: if indexPoints.length === 0, show "데이터 없음" in card

    2. **src/app/index/[clusterId]/page.tsx** (per D-12, D-13, D-18): Server component:
       - `export const revalidate = 3600;`
       - generateStaticParams: return CLUSTER_DEFINITIONS.map(c => ({ clusterId: c.id }))
       - generateMetadata: cluster name in title
       - Find cluster from CLUSTER_DEFINITIONS by params.clusterId, if not found call notFound()
       - Call computeClusterIndex(cluster.regionCodes)
       - Layout: max-w-6xl centered, breadcrumb (지역 지수 > 군집명), heading
       - Full ClusterIndexChart component with data
       - Below chart: 군집 내 구별 통계 section — for each regionCode in cluster.regionCodes, show region name from REGION_HIERARCHY lookup + latest month medianPrice. Use a simple `getPool().query()` to get latest 3-month median per region_code within the cluster. OR simpler: compute per-region index inline. Keep it lightweight — a grid of stat cards showing each gu's name + recent median price.

    3. **MobileNav.tsx**: Add `{ href: "/index", label: "지역 지수" }` to NAV_ITEMS array, after the "/market" entry (line ~11).
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - /index renders card grid with 4 cluster cards showing index + sparkline
    - /index/[clusterId] renders full LineChart with ReferenceLine at 100
    - MobileNav includes "지역 지수" link
    - next build succeeds
  </acceptance_criteria>
  <done>/index and /index/[clusterId] pages exist, build passes, MobileNav has 지역 지수 link</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `npx next build` — build succeeds with /index and /index/gangnam3 routes
3. Browser: /index shows 4 cluster cards with index values and sparklines
4. Browser: /index/gangnam3 shows full time series chart with ReferenceLine at 100
</verification>

<success_criteria>
- /index page displays 4 cluster cards (gangnam3, mayongseong, nodogang, sudobukmain)
- Each card shows: cluster name, current index (decimal), monthly change with arrow, sparkline
- Clicking card navigates to /index/[clusterId] with full LineChart
- ReferenceLine at y=100 visible on detail chart
- MobileNav has "지역 지수" menu item
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-regional-index-dashboard/15-01-SUMMARY.md`
</output>
