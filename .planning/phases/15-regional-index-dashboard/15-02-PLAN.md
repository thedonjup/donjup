---
phase: 15-regional-index-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/market/page.tsx
  - src/app/market/[sido]/page.tsx
autonomous: true
requirements: [INDEX-03]
must_haves:
  truths:
    - "market/page.tsx의 시도별 카드에 최근 3개월 중위가와 평균가가 표시된다"
    - "market/[sido]/page.tsx의 시군구별 카드에 최근 3개월 중위가와 평균가가 표시된다"
    - "중위가/평균가 산출에 이상거래(직거래)가 제외된다"
  artifacts:
    - path: "src/app/market/page.tsx"
      provides: "시도별 카드에 중위가+평균가 행 추가"
      contains: "중위가"
    - path: "src/app/market/[sido]/page.tsx"
      provides: "시군구별 카드에 중위가+평균가 행 추가"
      contains: "중위가"
  key_links:
    - from: "src/app/market/page.tsx"
      to: "apt_transactions"
      via: "최근 3개월 trade_price 쿼리"
      pattern: "trade_date.*gte"
    - from: "src/app/market/[sido]/page.tsx"
      to: "apt_transactions"
      via: "최근 3개월 trade_price 쿼리"
      pattern: "trade_date.*gte"
---

<objective>
기존 market 시세 페이지(시도별, 시군구별)에 최근 3개월 평균 매매가와 중위가를 추가한다.

Purpose: 투자자가 지역별 기본 가격 수준을 즉시 파악할 수 있게 한다.
Output: market/page.tsx, market/[sido]/page.tsx 카드에 중위가/평균가 표시
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-regional-index-dashboard/15-RESEARCH.md

@src/app/market/page.tsx
@src/app/market/[sido]/page.tsx
@src/lib/price-normalization.ts
@src/lib/format.ts

<interfaces>
From src/lib/price-normalization.ts:
```typescript
export function computeMedianPrice(prices: number[]): number;
export function isDirectDeal(dealType: string | null): boolean;
```

From src/lib/format.ts:
```typescript
export function formatPrice(priceInManWon: number): string;
```

market/page.tsx pattern: Uses `createClient()` (supabase QueryBuilder), Promise.all over sidoEntries, each with 3 parallel queries (count, topDrop, topHigh). Card grid: `grid gap-3 sm:grid-cols-2 lg:grid-cols-3`.

market/[sido]/page.tsx pattern: Same structure but per-sigungu. Each card has count, topDrop, topHigh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add median/avg price to sido and sigungu market pages</name>
  <files>
    src/app/market/page.tsx,
    src/app/market/[sido]/page.tsx
  </files>
  <read_first>
    src/app/market/page.tsx,
    src/app/market/[sido]/page.tsx,
    src/lib/price-normalization.ts,
    src/lib/format.ts
  </read_first>
  <action>
    **Both files follow the same modification pattern** (per D-19, D-20, D-21, D-22):

    1. **Add imports:** `import { computeMedianPrice, isDirectDeal } from "@/lib/price-normalization";`

    2. **Compute 3-month cutoff date** (per D-21):
       ```typescript
       const threeMonthsAgo = new Date();
       threeMonthsAgo.setMonth(threeMonthsAgo.getMonth() - 3);
       const cutoff = threeMonthsAgo.toISOString().split("T")[0];
       ```

    3. **Add 4th query to each Promise.all** (per RESEARCH pitfall 5 — add within existing Promise.all, NOT separate loop):
       - For market/page.tsx (sido level): query apt_transactions with `.in("region_code", sigunguCodes).gte("trade_date", cutoff).eq("property_type", 1)` (or use applyTypeFilter), select `trade_price, deal_type`
       - For market/[sido]/page.tsx (sigungu level): query apt_transactions with `.eq("region_code", code).gte("trade_date", cutoff)`, select `trade_price, deal_type`

    4. **Compute median and average in JS** (per D-22 — filter out direct deals):
       ```typescript
       const validPrices = (priceResult.data ?? [])
         .filter(t => !isDirectDeal(t.deal_type))
         .map(t => t.trade_price);
       const medianPrice = computeMedianPrice(validPrices);
       const avgPrice = validPrices.length
         ? Math.round(validPrices.reduce((a, b) => a + b, 0) / validPrices.length)
         : 0;
       ```

    5. **Extend stats type** to include `medianPrice: number` and `avgPrice: number`.

    6. **Add UI row below existing 2-column grid** in each card (per RESEARCH pattern 5):
       ```tsx
       <div className="mt-3 pt-3 border-t t-border flex gap-4 text-xs">
         <div>
           <p className="t-text-tertiary">최근 3개월 중위가</p>
           <p className="font-semibold t-text tabular-nums">{formatPrice(sido.medianPrice)}</p>
         </div>
         <div>
           <p className="t-text-tertiary">평균가</p>
           <p className="font-semibold t-text tabular-nums">{formatPrice(sido.avgPrice)}</p>
         </div>
       </div>
       ```
       Place this after the existing `grid grid-cols-2 gap-2` div that has topDrop/topHigh.

    7. **Handle zero prices:** If medianPrice === 0 AND avgPrice === 0, do NOT render the price row (no data for last 3 months).

    **IMPORTANT:** In market/page.tsx, the 4th query must also go through `applyTypeFilter` to respect the property type filter. In market/[sido]/page.tsx there is no property type filter currently, so just add `.eq("property_type", 1)` to the query.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - market/page.tsx card shows "최근 3개월 중위가" and "평균가" below the drop/high grid
    - market/[sido]/page.tsx card shows same price info per sigungu
    - Direct deals are excluded from median/avg calculation
    - Build succeeds
  </acceptance_criteria>
  <done>Both market pages display median and average prices, build passes</done>
</task>

</tasks>

<verification>
1. `npx next build` — build succeeds
2. Browser: /market shows 시도 cards with 중위가/평균가 row
3. Browser: /market/seoul shows 시군구 cards with 중위가/평균가 row
4. Prices are reasonable (not zero for major regions like Seoul)
</verification>

<success_criteria>
- /market page cards have "최근 3개월 중위가" and "평균가" displayed
- /market/[sido] page cards have same info per sigungu
- Direct deals excluded from calculations (per D-22)
- No N+1 query issues (single additional query per entity in existing Promise.all)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-regional-index-dashboard/15-02-SUMMARY.md`
</output>
