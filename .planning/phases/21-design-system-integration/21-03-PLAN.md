---
phase: 21-design-system-integration
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/components/charts/PriceHistoryChart.tsx
  - src/components/charts/ClusterIndexChart.tsx
  - src/app/global-error.tsx
autonomous: false
requirements: [DESIGN-01, DESIGN-03, DESIGN-04]
must_haves:
  truths:
    - "Chart lines and scatter dots use CSS variable colors that change with dark mode toggle"
    - "PriceHistoryChart tooltip ratio label uses CSS variable color"
    - "global-error.tsx hardcoded colors are annotated as intentional design-system exceptions"
    - "All non-exception, non-OG component files have zero hardcoded hex colors"
  artifacts:
    - path: "src/components/charts/PriceHistoryChart.tsx"
      provides: "Chart with CSS variable stroke/fill colors"
      contains: "var(--color-chart-"
    - path: "src/components/charts/ClusterIndexChart.tsx"
      provides: "Cluster chart with CSS variable stroke color"
      contains: "var(--color-chart-"
  key_links:
    - from: "src/components/charts/PriceHistoryChart.tsx"
      to: "src/app/globals.css"
      via: "CSS variable references in SVG attributes"
      pattern: "var\\(--color-chart-"
    - from: "src/components/charts/ClusterIndexChart.tsx"
      to: "src/app/globals.css"
      via: "CSS variable references in SVG attributes"
      pattern: "var\\(--color-chart-"
---

<objective>
Replace hardcoded hex colors in Recharts SVG attributes with CSS variables, annotate global-error.tsx exceptions, and perform final visual verification of dark mode.

Purpose: Complete DESIGN-01 (zero hardcoded colors), DESIGN-03 (inline style elimination), and DESIGN-04 (dynamic color dark mode). Charts are the last remaining source of hardcoded colors.
Output: 2 chart files + 1 error file updated. Visual dark mode checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-design-system-integration/21-RESEARCH.md
@.planning/phases/21-design-system-integration/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace chart SVG hardcoded colors with CSS variables</name>
  <files>src/components/charts/PriceHistoryChart.tsx, src/components/charts/ClusterIndexChart.tsx</files>
  <read_first>src/components/charts/PriceHistoryChart.tsx, src/components/charts/ClusterIndexChart.tsx, src/app/globals.css</read_first>
  <action>
**PriceHistoryChart.tsx** — Replace ALL hardcoded stroke/fill SVG attributes:

1. `stroke="#9CA3AF"` (line ~92, CartesianGrid) → `stroke="var(--color-chart-neutral)"`
2. `fill="#9CA3AF"` (line ~101, XAxis tick) → `fill="var(--color-chart-neutral)"`
3. `fill="#059669"` (line ~394, Scatter for sale dots) → `fill="var(--color-chart-sale)"`
4. `fill="#9CA3AF"` (line ~403, Scatter for direct trade dots) → `fill="var(--color-chart-neutral)"`
5. `stroke="#059669"` (line ~414, Line for sale trend) → `stroke="var(--color-chart-sale)"`
6. `stroke="#059669"` (line ~428, another sale line reference) → `stroke="var(--color-chart-sale)"`
7. `stroke="#3B82F6"` (line ~444, Line for jeonse trend) → `stroke="var(--color-chart-jeonse)"`
8. `stroke="#3B82F6"` (line ~460, another jeonse line reference) → `stroke="var(--color-chart-jeonse)"`
9. `stroke="#F97316"` (line ~477, Line for jeonse ratio) → `stroke="var(--color-chart-ratio)"`
10. `style={{ color: "#F97316" }}` (line ~238, tooltip ratio label) → `style={{ color: "var(--color-chart-ratio)" }}`

Also check for any `activeDot` props that contain hardcoded colors — replace with CSS vars if found.

**ClusterIndexChart.tsx** — Replace:
1. `stroke="#9CA3AF"` (line ~116, CartesianGrid) → `stroke="var(--color-chart-neutral)"`
2. `stroke="#2B579A"` (line ~128, Line for cluster index) → `stroke="var(--color-chart-index)"`
3. `style={{ color: "#2B579A" }}` (line ~55, tooltip label) → `style={{ color: "var(--color-chart-index)" }}`

**Important:** SVG attributes accept CSS variable values directly. Do NOT use getComputedStyle — set `var(--color-...)` strings directly in the JSX attributes. If Recharts legend color swatches don't render CSS vars, use a `legendPayload` override or accept the limitation (note in comments).
  </action>
  <verify>
    <automated>grep -n '#[0-9a-fA-F]\{6\}' src/components/charts/PriceHistoryChart.tsx src/components/charts/ClusterIndexChart.tsx | grep -v "var(--" | grep -v "//" && echo "FAIL: hardcoded colors remain" || echo "PASS" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'stroke="#' src/components/charts/PriceHistoryChart.tsx` returns 0
    - `grep -c 'fill="#' src/components/charts/PriceHistoryChart.tsx` returns 0
    - `grep -c 'color: "#' src/components/charts/PriceHistoryChart.tsx` returns 0
    - `grep -c 'stroke="#' src/components/charts/ClusterIndexChart.tsx` returns 0
    - `grep -c 'color: "#' src/components/charts/ClusterIndexChart.tsx` returns 0
    - All replacements use `var(--color-chart-sale)`, `var(--color-chart-jeonse)`, `var(--color-chart-ratio)`, `var(--color-chart-neutral)`, `var(--color-chart-index)` CSS variables
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Both chart files use CSS variable references for all stroke/fill colors, enabling automatic dark mode color adaptation</done>
</task>

<task type="auto">
  <name>Task 2: Annotate global-error.tsx exceptions + final hardcoded color audit</name>
  <files>src/app/global-error.tsx</files>
  <read_first>src/app/global-error.tsx</read_first>
  <action>
1. In `src/app/global-error.tsx`, add a comment at the top of the component (after imports):
   ```typescript
   // design-system-exception: global-error renders outside ThemeProvider and layout.tsx
   // Hardcoded colors are intentional — CSS variables may not be available in error state
   ```

2. Run a final audit across the entire `src/` directory to confirm no remaining hardcoded hex colors in inline styles, excluding known exceptions:
   - `opengraph-image.tsx` files (server-rendered static images, no dark mode)
   - `global-error.tsx` (annotated exception)
   - `layout.tsx` meta theme-color (browser chrome color, not UI)
   - `cardnews` files (if any — static image generation)
   - Brand-specific colors annotated with `// brand:` comments

3. If any remaining hardcoded colors are found outside exceptions, fix them using appropriate CSS variables.

4. Run `npm run build` and `npm test` to confirm no regressions.
  </action>
  <verify>
    <automated>grep -rn 'color: "#\|background: "#\|backgroundColor: "#\|stroke="#\|fill="#' src/ --include="*.tsx" --include="*.ts" | grep -v opengraph | grep -v global-error | grep -v layout.tsx | grep -v cardnews | grep -v "brand:" | grep -v node_modules | grep -v "\.test\." && echo "FAIL: remaining hardcoded colors" || echo "PASS: all hardcoded colors resolved" && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `global-error.tsx` contains `design-system-exception` comment
    - The audit grep (excluding opengraph, global-error, layout.tsx, cardnews, brand:) returns 0 matches
    - `npm run build` succeeds
    - `npm test` passes
  </acceptance_criteria>
  <done>global-error.tsx annotated as design-system exception, final audit confirms zero non-exception hardcoded hex colors across codebase</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual dark mode verification</name>
  <files>N/A</files>
  <action>
Deploy to production with `npx vercel --prod --yes`, then request user to visually verify dark mode across all pages.
  </action>
  <what-built>Complete dark mode design system: @custom-variant dark enables Tailwind dark: utilities, all hardcoded hex colors replaced with CSS variables, DROP_LEVEL_CONFIG centralized, chart colors respond to theme toggle</what-built>
  <how-to-verify>
    1. Visit https://donjup.com
    2. Toggle dark mode using the theme switcher
    3. Check these pages in both light and dark mode:
       - Home page (/) — RankingTabs drop level badges should have readable colors
       - Today page (/today) — drop level badges in dark mode
       - Any apartment detail page (/apt/...) — TransactionTabs, price chart
       - Market page (/market) — ClusterIndexChart colors
       - Rate page (/rate) — verify no white-on-white or dark-on-dark text
    4. Verify PriceHistoryChart: sale line (green), jeonse line (blue), ratio line (orange) all visible in dark mode
    5. Verify login modal (click login button) — text readable in dark mode
    6. Check admin pages (/dam) — sidebar and content area dark mode
  </how-to-verify>
  <verify>User visual confirmation of dark mode rendering</verify>
  <done>User has visually confirmed dark mode works correctly across all pages</done>
  <resume-signal>Type "approved" or describe specific dark mode issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -rn '#[0-9a-fA-F]\{6\}' src/components/charts/ --include="*.tsx" | grep -v "var(--" | grep -v "//"` returns 0 matches
2. Full codebase audit (excluding exceptions) returns 0 hardcoded hex colors
3. `npm run build` succeeds
4. `npm test` passes
5. Visual dark mode verification approved by user
</verification>

<success_criteria>
- PriceHistoryChart and ClusterIndexChart use CSS variables for all stroke/fill/color
- global-error.tsx annotated as design-system exception
- Final codebase audit: zero non-exception hardcoded hex colors
- Dark mode visual verification: all pages render correctly with theme toggle
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-system-integration/21-03-SUMMARY.md`
</output>
