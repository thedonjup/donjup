---
phase: 21-design-system-integration
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/components/apt/TransactionTabs.tsx
  - src/components/apt/AptDetailClient.tsx
  - src/components/home/RankingTabs.tsx
  - src/components/auth/LoginModal.tsx
  - src/components/auth/UserMenu.tsx
  - src/components/ShareButtons.tsx
  - src/components/onboarding/RegionSelector.tsx
  - src/app/dam/AdminLayout.tsx
  - src/app/dam/content/page.tsx
  - src/app/dam/users/page.tsx
  - src/app/dam/cron/page.tsx
autonomous: true
requirements: [DESIGN-01, DESIGN-03]
must_haves:
  truths:
    - "No hardcoded hex color values remain in component inline styles (excluding opengraph, global-error, layout.tsx meta)"
    - "All inline style color values reference CSS variables or use Tailwind classes"
    - "Tab active states use CSS variable references instead of hardcoded #fff"
  artifacts:
    - path: "src/components/apt/TransactionTabs.tsx"
      provides: "Tab component with CSS var colors"
      contains: "var(--color-"
    - path: "src/components/auth/LoginModal.tsx"
      provides: "Login modal with CSS var colors (except brand-specific Kakao/Naver)"
      contains: "var(--color-"
  key_links:
    - from: "src/components/apt/TransactionTabs.tsx"
      to: "src/app/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--color-"
    - from: "src/components/home/RankingTabs.tsx"
      to: "src/app/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--color-"
---

<objective>
Sweep all component files to replace hardcoded hex colors in inline styles with CSS variable references or Tailwind classes. This covers non-chart, non-OG image components.

Purpose: DESIGN-01 requires zero hardcoded colors for dark mode compliance. DESIGN-03 requires inline style elimination where possible.
Output: 11 component files updated, zero hardcoded hex in inline styles (excl. opengraph, global-error, layout meta).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-design-system-integration/21-RESEARCH.md
@.planning/phases/21-design-system-integration/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex colors in apt/home/onboarding components</name>
  <files>src/components/apt/TransactionTabs.tsx, src/components/apt/AptDetailClient.tsx, src/components/home/RankingTabs.tsx, src/components/onboarding/RegionSelector.tsx</files>
  <read_first>src/components/apt/TransactionTabs.tsx, src/components/apt/AptDetailClient.tsx, src/components/home/RankingTabs.tsx, src/components/onboarding/RegionSelector.tsx</read_first>
  <action>
**TransactionTabs.tsx** — Replace these hardcoded colors:
- `color: "#fff"` (6 instances, lines ~94/107/131/140/158/167) → `color: "var(--color-text-inverted)"`
- `background: "rgba(255,255,255,0.25)"` (2 instances) → `background: "rgba(255,255,255,0.25)"` (keep — this is a white overlay opacity, acceptable)
- The DROP_LEVEL_CONFIG local definition was removed in Plan 01 Task 2. Verify import exists.

**AptDetailClient.tsx** — Replace these hardcoded colors:
- `color: "#fff"` (3 instances, lines ~317/337/401) → `color: "var(--color-text-inverted)"`
- Keep `background: "var(--color-brand-600)"` as-is (already CSS var)

**RankingTabs.tsx** — Replace:
- `color: "#6b7280"` (line ~241) → `color: "var(--color-text-tertiary)"` (slate-500 maps to text-tertiary in dark)
- The DROP_LEVEL_CONFIG local definition was removed in Plan 01 Task 2. Verify import exists.

**RegionSelector.tsx** — Replace:
- `color: "#fff"` (line ~188) → `color: "var(--color-text-inverted)"`
  </action>
  <verify>
    <automated>grep -n 'color: "#' src/components/apt/TransactionTabs.tsx src/components/apt/AptDetailClient.tsx src/components/home/RankingTabs.tsx src/components/onboarding/RegionSelector.tsx | grep -v "var(--" && echo "FAIL" || echo "PASS: no hardcoded colors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'color: "#' src/components/apt/TransactionTabs.tsx` returns 0 matches
    - `grep 'color: "#' src/components/apt/AptDetailClient.tsx` returns 0 matches
    - `grep 'color: "#' src/components/home/RankingTabs.tsx` returns 0 matches
    - `grep 'color: "#' src/components/onboarding/RegionSelector.tsx` returns 0 matches
    - All replaced values use `var(--color-text-inverted)` or `var(--color-text-tertiary)`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>4 component files have zero hardcoded hex colors in inline styles, all replaced with CSS variable references</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex colors in auth/share/dam components</name>
  <files>src/components/auth/LoginModal.tsx, src/components/auth/UserMenu.tsx, src/components/ShareButtons.tsx, src/app/dam/AdminLayout.tsx, src/app/dam/content/page.tsx, src/app/dam/users/page.tsx, src/app/dam/cron/page.tsx</files>
  <read_first>src/components/auth/LoginModal.tsx, src/components/auth/UserMenu.tsx, src/components/ShareButtons.tsx, src/app/dam/AdminLayout.tsx, src/app/dam/content/page.tsx, src/app/dam/users/page.tsx, src/app/dam/cron/page.tsx</read_first>
  <action>
**LoginModal.tsx** — Replace/annotate:
- `background: "#059669"` (line ~124, brand logo icon) → `background: "var(--color-semantic-rise)"` (brand green)
- `color: "#333"` (line ~147, "돈줍 시작하기" heading) → `color: "var(--color-text-primary)"`
- `color: "#391B1B"` (line ~172, Kakao button text) → keep as-is, add comment `// brand: Kakao login text color`
- `background: "#FEE500"` (line ~171, Kakao button) → keep as-is, add comment `// brand: Kakao yellow`
- `background: "#03C75A"` (line ~189, Naver button) → keep as-is, add comment `// brand: Naver green`
- `color: "#dc2626"` (line ~202, error text) → `color: "var(--color-semantic-drop)"`

**UserMenu.tsx** — Replace:
- `background: "#059669"` (2 instances, lines ~66/114) → `background: "var(--color-semantic-rise)"`
- `color: "#059669"` (line ~189) → `color: "var(--color-semantic-rise)"`

**ShareButtons.tsx** — Replace/annotate:
- `background: "#FEE500"` (line ~94, Kakao share) → keep as-is, add comment `// brand: Kakao yellow`
- `color: "#3C1E1E"` (line ~93, Kakao share text) → keep as-is, add comment `// brand: Kakao text`

**AdminLayout.tsx** — Replace:
- `background: "#1e293b"` (line ~74) → `background: "var(--color-hero-via)"` (same value, already defined)
- `background: "#1e293b"` (line ~133) → `background: "var(--color-hero-via)"`
- `borderColor: "#334155"` (line ~133) → `borderColor: "var(--color-border)"` or add `--color-admin-border: #334155` to globals.css if no match

**dam/content/page.tsx** — Replace:
- `bg: "#dbeafe", color: "#2563eb"` (line ~67, posted status badge) → `bg: "var(--color-semantic-info-bg, #dbeafe)", color: "var(--color-semantic-info, #2563eb)"` — OR add CSS vars `--color-status-posted-bg: #dbeafe; --color-status-posted: #2563eb;` to :root in globals.css
- `background: "#2563eb"` (line ~156, post button) → `background: "var(--color-brand-600)"`

**dam/users/page.tsx** — Replace:
- `background: "#f59e0b"` (line ~97) → `background: "var(--color-semantic-warn)"`

**dam/cron/page.tsx** — Replace:
- `background: "#2563eb"` (line ~152) → `background: "var(--color-brand-600)"`

For any new CSS variables needed (e.g., admin border, status posted), add to globals.css `:root` and `[data-theme="dark"]` blocks.

Brand-specific colors (Kakao #FEE500, Naver #03C75A, Kakao text #3C1E1E/#391B1B) are intentional exceptions — annotate with `// brand:` comments.
  </action>
  <verify>
    <automated>grep -rn 'color: "#\|background: "#\|backgroundColor: "#' src/components/auth/LoginModal.tsx src/components/auth/UserMenu.tsx src/components/ShareButtons.tsx src/app/dam/AdminLayout.tsx src/app/dam/content/page.tsx src/app/dam/users/page.tsx src/app/dam/cron/page.tsx | grep -v "brand:" | grep -v "var(--" && echo "FAIL" || echo "PASS" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'color: "#' src/components/auth/UserMenu.tsx` returns 0 matches
    - `grep 'background: "#' src/app/dam/AdminLayout.tsx` returns 0 matches
    - `grep 'background: "#' src/app/dam/users/page.tsx` returns 0 matches
    - LoginModal.tsx and ShareButtons.tsx only retain brand-specific colors (Kakao/Naver) with `// brand:` comments
    - `npm run build` succeeds
    - `npm test` passes
  </acceptance_criteria>
  <done>7 auth/share/dam component files have zero non-brand hardcoded hex colors, all replaced with CSS variables. Kakao/Naver brand colors annotated as intentional exceptions.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'color: "#[0-9a-fA-F]' src/ --include="*.tsx" | grep -v opengraph | grep -v global-error | grep -v layout.tsx | grep -v "brand:" | grep -v "cardnews"` returns only chart files (handled in Plan 03)
2. `npm run build` succeeds
3. `npm test` passes
</verification>

<success_criteria>
- Zero hardcoded hex colors in inline styles across 11 component files (excl. brand-specific Kakao/Naver)
- All replacements use existing CSS variables from globals.css
- Brand-specific colors (Kakao yellow, Naver green) annotated with // brand: comments
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-system-integration/21-02-SUMMARY.md`
</output>
