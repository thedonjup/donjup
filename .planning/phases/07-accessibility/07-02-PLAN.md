---
phase: 07-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/charts/PriceHistoryChart.tsx
  - src/components/charts/MiniAreaChart.tsx
  - src/components/charts/MiniAreaChartWrapper.tsx
  - src/components/charts/PriceHistoryChartWrapper.tsx
  - src/components/map/KakaoMap.tsx
autonomous: true
requirements: [A11Y-03]

must_haves:
  truths:
    - "차트에 aria-label이 존재하여 스크린 리더가 차트의 목적을 전달할 수 있다"
    - "차트 옆 또는 아래에 텍스트 요약이 제공되어 시각 장애인이 데이터를 파악할 수 있다"
    - "지도 컨테이너에 대안 텍스트가 있어 스크린 리더가 지도의 존재와 목적을 전달한다"
  artifacts:
    - path: "src/components/charts/PriceHistoryChart.tsx"
      provides: "차트 aria-label + 텍스트 요약"
      contains: "aria-label"
    - path: "src/components/charts/MiniAreaChart.tsx"
      provides: "미니 차트 aria-label"
      contains: "aria-label"
    - path: "src/components/map/KakaoMap.tsx"
      provides: "지도 대안 텍스트"
      contains: "aria-label"
  key_links:
    - from: "src/components/charts/PriceHistoryChart.tsx"
      to: "screen reader"
      via: "aria-label on chart container + sr-only text summary"
      pattern: "sr-only"
    - from: "src/components/map/KakaoMap.tsx"
      to: "screen reader"
      via: "aria-label on map container + sr-only description"
      pattern: "aria-label"
---

<objective>
차트와 지도에 텍스트 대안을 제공하여 보조 기술 사용자가 데이터를 파악할 수 있게 한다.

Purpose: 시각적 차트/지도를 볼 수 없는 사용자에게 동등한 정보 접근성을 제공한다.
Output: aria-label + sr-only 텍스트 요약이 적용된 차트/지도 컴포넌트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 차트 컴포넌트 aria-label + 텍스트 요약</name>
  <files>src/components/charts/PriceHistoryChart.tsx, src/components/charts/MiniAreaChart.tsx, src/components/charts/MiniAreaChartWrapper.tsx, src/components/charts/PriceHistoryChartWrapper.tsx</files>
  <read_first>src/components/charts/PriceHistoryChart.tsx, src/components/charts/MiniAreaChart.tsx, src/components/charts/MiniAreaChartWrapper.tsx, src/components/charts/PriceHistoryChartWrapper.tsx</read_first>
  <action>
Per A11Y-03:

**PriceHistoryChart.tsx:**
- 차트를 감싸는 최상위 div에 `role="figure"`, `aria-label="가격 추이 차트"` 추가
- 차트 아래에 sr-only 텍스트 요약 추가: `<p className="sr-only">`에 데이터의 최저가, 최고가, 기간을 텍스트로 표시
- 요약 예: "2025-01 ~ 2025-12 기간 중 최저 {formatPrice(min)} ~ 최고 {formatPrice(max)}, 총 {data.length}건의 거래"
- data 배열에서 min/max price를 계산하여 요약 생성:
  ```
  const minPrice = Math.min(...data.map(d => d.price));
  const maxPrice = Math.max(...data.map(d => d.price));
  const dateRange = `${data[0].fullDate} ~ ${data[data.length-1].fullDate}`;
  ```

**MiniAreaChart.tsx:**
- props에 `label?: string` 추가 (기본값: "가격 추이 미니 차트")
- 감싸는 div 추가: `<div role="img" aria-label={label}>`
- 호출부에서 label을 전달하도록 MiniAreaChartWrapper.tsx 수정 (존재하는 경우)

**PriceHistoryChartWrapper.tsx (존재하는 경우):**
- 동일하게 aria-label 전파 확인

주의: Recharts의 ResponsiveContainer는 SVG를 렌더링하므로 감싸는 div에 role/aria-label을 적용하는 것이 올바른 패턴이다. SVG 자체에 직접 접근할 수 없으므로 container 레벨에서 처리.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -c 'aria-label' src/components/charts/PriceHistoryChart.tsx && grep -c 'sr-only' src/components/charts/PriceHistoryChart.tsx && grep -c 'aria-label' src/components/charts/MiniAreaChart.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
- `grep 'aria-label' src/components/charts/PriceHistoryChart.tsx` returns match
- `grep 'sr-only' src/components/charts/PriceHistoryChart.tsx` returns match (텍스트 요약)
- `grep 'role="figure"\|role="img"' src/components/charts/PriceHistoryChart.tsx` returns match
- `grep 'aria-label' src/components/charts/MiniAreaChart.tsx` returns match
- `pnpm build` succeeds
  </acceptance_criteria>
  <done>PriceHistoryChart에 aria-label + sr-only 텍스트 요약(기간/최저가/최고가), MiniAreaChart에 role=img + aria-label 적용</done>
</task>

<task type="auto">
  <name>Task 2: 지도 컴포넌트 대안 텍스트</name>
  <files>src/components/map/KakaoMap.tsx</files>
  <read_first>src/components/map/KakaoMap.tsx</read_first>
  <action>
Per A11Y-03:

**KakaoMap.tsx:**
- 지도 컨테이너 div (mapRef)에 `role="application"`, `aria-label="전국 아파트 실거래가 지도"` 추가
- `aria-roledescription="인터랙티브 지도"` 추가하여 스크린 리더가 지도임을 명확히 전달
- 지도 컨테이너 바로 앞에 sr-only 설명 추가:
  ```
  <p className="sr-only">
    카카오맵 기반 전국 아파트 실거래가 지도입니다.
    현재 {filteredTransactions.length}건의 거래가 표시되어 있습니다.
    지도 조작은 마우스 또는 터치로 가능합니다.
  </p>
  ```
- 필터 칩 그룹(전체/폭락/신고가)을 감싸는 div에 `role="group"`, `aria-label="지도 필터"` 추가
- SDK 로딩 중 표시되는 로딩 상태에도 `aria-live="polite"` 추가하여 로딩 완료 시 알림

주의: role="application"은 지도처럼 자체 키보드 인터랙션을 가진 위젯에 적합한 role이다. 일반적인 콘텐츠에는 사용하지 않지만 카카오맵은 자체 키보드/마우스 핸들러를 가지므로 적합.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -c 'aria-label' src/components/map/KakaoMap.tsx && grep -c 'sr-only' src/components/map/KakaoMap.tsx && grep -c 'role=' src/components/map/KakaoMap.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
- `grep 'aria-label.*지도' src/components/map/KakaoMap.tsx` returns match
- `grep 'sr-only' src/components/map/KakaoMap.tsx` returns match (대안 텍스트)
- `grep 'role="application"' src/components/map/KakaoMap.tsx` returns match
- `grep 'aria-live' src/components/map/KakaoMap.tsx` returns match
- `pnpm build` succeeds
  </acceptance_criteria>
  <done>지도 컨테이너에 role=application + aria-label + sr-only 설명 텍스트, 필터 그룹에 role=group, 로딩 상태에 aria-live 적용</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- `grep -r 'aria-label' src/components/charts/` 최소 2건
- `grep -r 'sr-only' src/components/charts/PriceHistoryChart.tsx` 1건 이상
- `grep -r 'aria-label' src/components/map/KakaoMap.tsx` 1건 이상
- `grep -r 'sr-only' src/components/map/KakaoMap.tsx` 1건 이상
</verification>

<success_criteria>
- 모든 차트에 aria-label 존재
- PriceHistoryChart에 sr-only 텍스트 요약 (기간, 최저/최고가)
- 지도에 role=application + aria-label + sr-only 설명
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-02-SUMMARY.md`
</output>
