---
phase: 07-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/RankingTabs.tsx
  - src/components/apt/TransactionTabs.tsx
  - src/components/charts/PriceHistoryChart.tsx
  - src/components/map/FilterChip.tsx
  - src/components/layout/MobileNav.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [A11Y-01, A11Y-02, A11Y-04]

must_haves:
  truths:
    - "탭 컴포넌트에 role=tablist/tab/tabpanel과 aria-selected가 존재한다"
    - "키보드(Tab, Enter, Escape)로 주요 인터랙티브 요소를 조작할 수 있다"
    - "모바일 내비 드로어가 Escape 키로 닫히고 포커스 트랩이 동작한다"
    - "skip-to-content 링크가 layout.tsx에 존재하고 #main-content로 이동한다"
  artifacts:
    - path: "src/components/home/RankingTabs.tsx"
      provides: "ARIA tablist/tab/tabpanel 패턴"
      contains: "role=\"tablist\""
    - path: "src/components/apt/TransactionTabs.tsx"
      provides: "ARIA tablist/tab/tabpanel 패턴"
      contains: "role=\"tab\""
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Escape 키 핸들러 + role=dialog"
      contains: "role=\"dialog\""
    - path: "src/app/layout.tsx"
      provides: "skip-to-content 링크 (이미 존재, 검증만)"
      contains: "skip"
  key_links:
    - from: "src/components/home/RankingTabs.tsx"
      to: "tab button click"
      via: "aria-selected + role=tab"
      pattern: "aria-selected"
    - from: "src/components/layout/MobileNav.tsx"
      to: "keyboard escape"
      via: "onKeyDown Escape handler"
      pattern: "Escape"
---

<objective>
주요 인터랙티브 요소에 ARIA 속성을 추가하고 키보드 내비게이션을 구현한다.

Purpose: 보조 기술(스크린 리더) 사용자가 탭, 버튼, 드로어를 인식하고 조작할 수 있도록 한다.
Output: ARIA role/속성이 적용된 컴포넌트 + 키보드 내비게이션 동작
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 탭 컴포넌트 ARIA 속성 + 키보드 내비게이션</name>
  <files>src/components/home/RankingTabs.tsx, src/components/apt/TransactionTabs.tsx, src/components/charts/PriceHistoryChart.tsx, src/components/map/FilterChip.tsx</files>
  <read_first>src/components/home/RankingTabs.tsx, src/components/apt/TransactionTabs.tsx, src/components/charts/PriceHistoryChart.tsx, src/components/map/FilterChip.tsx</read_first>
  <action>
Per A11Y-01 and A11Y-02:

**RankingTabs.tsx:**
- 탭 컨테이너 div에 `role="tablist"` `aria-label="거래 랭킹"` 추가
- 각 탭 button에 `role="tab"`, `aria-selected={activeTab === tab.key}`, `id={`tab-${tab.key}`}`, `aria-controls={`tabpanel-${tab.key}`}` 추가
- 탭 콘텐츠 영역 div (line 124, `<div className="mt-4 space-y-2">`)를 감싸는 div에 `role="tabpanel"`, `id={`tabpanel-${activeTab}`}`, `aria-labelledby={`tab-${activeTab}`}` 추가
- 탭 컨테이너에 onKeyDown 핸들러 추가: ArrowLeft/ArrowRight로 탭 간 이동, Home으로 첫 탭, End로 마지막 탭 이동
- `tabIndex={activeTab === tab.key ? 0 : -1}` 추가하여 roving tabindex 구현

**TransactionTabs.tsx:**
- 탭 버튼 컨테이너에 `role="tablist"` `aria-label="거래 이력"` 추가
- 매매/전월세 버튼에 `role="tab"`, `aria-selected={tab === "sale"}` / `aria-selected={tab === "rent"}`, `id="tab-sale"` / `id="tab-rent"`, `aria-controls="tabpanel-sale"` / `aria-controls="tabpanel-rent"` 추가
- 테이블 감싸는 div에 `role="tabpanel"`, `id={`tabpanel-${tab}`}`, `aria-labelledby={`tab-${tab}`}` 추가
- ArrowLeft/ArrowRight 키보드 핸들러 추가

**PriceHistoryChart.tsx - SizeTab:**
- SizeTab 버튼 그룹에 `role="tablist"` `aria-label="면적 필터"` 추가
- 각 SizeTab에 `role="tab"`, `aria-selected={active}` 추가

**FilterChip.tsx:**
- `aria-pressed={active}` 추가 (toggle button 패턴)
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx grep -r "role=\"tablist\"" src/components/home/RankingTabs.tsx src/components/apt/TransactionTabs.tsx && npx grep -r "aria-selected" src/components/home/RankingTabs.tsx src/components/apt/TransactionTabs.tsx && npx grep -r "aria-pressed" src/components/map/FilterChip.tsx</automated>
  </verify>
  <acceptance_criteria>
- `grep -r 'role="tablist"' src/components/home/RankingTabs.tsx` returns match
- `grep -r 'role="tablist"' src/components/apt/TransactionTabs.tsx` returns match
- `grep -r 'aria-selected' src/components/home/RankingTabs.tsx` returns match
- `grep -r 'aria-selected' src/components/apt/TransactionTabs.tsx` returns match
- `grep -r 'aria-pressed' src/components/map/FilterChip.tsx` returns match
- `grep -r 'ArrowRight\|ArrowLeft' src/components/home/RankingTabs.tsx` returns match
- `pnpm build` succeeds
  </acceptance_criteria>
  <done>RankingTabs, TransactionTabs에 WAI-ARIA tablist 패턴 적용, PriceHistoryChart SizeTab에 aria-selected, FilterChip에 aria-pressed, 키보드 좌우 화살표 탭 전환 동작</done>
</task>

<task type="auto">
  <name>Task 2: 모바일 내비 드로어 접근성 + skip-to-content 검증</name>
  <files>src/components/layout/MobileNav.tsx, src/app/layout.tsx</files>
  <read_first>src/components/layout/MobileNav.tsx, src/app/layout.tsx</read_first>
  <action>
Per A11Y-02 and A11Y-04:

**MobileNav.tsx:**
- 드로어 div에 `role="dialog"`, `aria-modal="true"`, `aria-label="내비게이션 메뉴"` 추가
- Escape 키 핸들러 추가: `useEffect`로 open 상태일 때 keydown 'Escape' 리스너 등록하여 `setOpen(false)` 호출
- 드로어 열릴 때 닫기 버튼에 자동 포커스 (useRef + useEffect로 open 변경 시 closeButtonRef.current?.focus())
- 드로어 닫힐 때 햄버거 버튼으로 포커스 복귀 (openButtonRef를 HamburgerButton에 전달하여 close 시 focus)
- Backdrop div에 `aria-hidden="true"` 추가

**layout.tsx (A11Y-04 — 검증만):**
- skip-to-content 링크가 이미 존재함 (line 108-113, `href="#main-content"`, text "본문으로 건너뛰기")
- `<main id="main-content">` 존재함 (line 142)
- 추가 작업 불필요, 존재 검증만 수행
- nav 요소 (line 169)에 `aria-label="주요 내비게이션"` 추가
- footer 요소에 `aria-label="사이트 정보"` 추가
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -c 'role="dialog"' src/components/layout/MobileNav.tsx && grep -c 'Escape' src/components/layout/MobileNav.tsx && grep -c 'skip' src/app/layout.tsx && grep -c 'main-content' src/app/layout.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
- `grep 'role="dialog"' src/components/layout/MobileNav.tsx` returns match
- `grep 'Escape' src/components/layout/MobileNav.tsx` returns match
- `grep 'aria-modal' src/components/layout/MobileNav.tsx` returns match
- `grep '#main-content' src/app/layout.tsx` returns match (skip link)
- `grep 'id="main-content"' src/app/layout.tsx` returns match (main landmark)
- `grep 'aria-label="주요 내비게이션"' src/app/layout.tsx` returns match
- `pnpm build` succeeds
  </acceptance_criteria>
  <done>모바일 드로어에 dialog role + Escape 닫기 + 포커스 관리 적용, skip-to-content 링크 존재 확인, header nav/footer에 aria-label 적용</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- `grep -r 'role="tablist"' src/components/` 최소 3건 (RankingTabs, TransactionTabs, PriceHistoryChart)
- `grep -r 'role="dialog"' src/components/layout/MobileNav.tsx` 1건
- `grep -r 'aria-selected' src/components/` 최소 2건
- `grep -r 'Escape' src/components/layout/MobileNav.tsx` 1건 이상
</verification>

<success_criteria>
- 모든 탭 컴포넌트에 WAI-ARIA tablist 패턴 적용
- 키보드(ArrowLeft/Right)로 탭 전환 가능
- 모바일 드로어가 Escape로 닫히고 포커스 관리됨
- skip-to-content 링크 존재 확인
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-01-SUMMARY.md`
</output>
