---
phase: 16-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - tests/unit/cluster-index.test.ts
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "computeClusterIndex에 대한 유닛 테스트가 존재하고 통과한다"
    - "DB 의존성이 vi.mock으로 격리되어 실제 DB 연결 없이 테스트가 실행된다"
    - "빈 입력, 정상 데이터, 엣지 케이스가 모두 커버된다"
  artifacts:
    - path: "tests/unit/cluster-index.test.ts"
      provides: "Unit tests for computeClusterIndex with DB mock"
      min_lines: 60
  key_links:
    - from: "tests/unit/cluster-index.test.ts"
      to: "src/lib/cluster-index.ts"
      via: "import with @/ alias"
      pattern: "import.*@/lib/cluster-index"
    - from: "tests/unit/cluster-index.test.ts"
      to: "src/lib/db/client.ts"
      via: "vi.mock for getPool"
      pattern: "vi\\.mock.*@/lib/db/client"
---

<objective>
computeClusterIndex 함수에 대한 유닛 테스트를 작성한다. DB 의존성을 vi.mock으로 격리하여 순수 로직만 검증한다.

Purpose: 군집 지수 계산 로직의 정확성을 DB 없이 검증할 수 있게 한다.
Output: tests/unit/cluster-index.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-test-infrastructure/16-RESEARCH.md
@.planning/phases/16-test-infrastructure/16-01-SUMMARY.md

@src/lib/cluster-index.ts
@src/lib/db/client.ts
@src/lib/price-normalization.ts

<interfaces>
<!-- From src/lib/cluster-index.ts -->
```typescript
export interface ClusterIndexPoint {
  month: string;
  index: number;
  medianPrice: number;
  count: number;
}

export async function computeClusterIndex(
  regionCodes: string[],
  minTransactions?: number  // default 3
): Promise<ClusterIndexPoint[]>;
```

<!-- From src/lib/db/client.ts -->
```typescript
export function getPool(): Pool;  // pg.Pool instance
```

<!-- computeClusterIndex 내부 동작 -->
1. regionCodes가 빈 배열이면 즉시 [] 반환
2. getPool().query()로 apt_transactions 조회
3. 직거래(deal_type='직거래') 필터링 제거
4. groupByMonth로 월별 그룹화
5. 첫 번째 minTransactions 이상인 월을 기준월(base)로 설정
6. 각 월의 중위가를 기준월 중위가로 나눠 지수 계산 (base=100)
7. minTransactions 미만인 월은 skip
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeClusterIndex 유닛 테스트 작성</name>
  <files>tests/unit/cluster-index.test.ts</files>
  <read_first>src/lib/cluster-index.ts, src/lib/db/client.ts</read_first>
  <behavior>
    --- 빈 입력 ---
    - regionCodes=[] -> [] 반환 (DB 호출 없음)

    --- 정상 데이터 (3개월, 각 월 3건 이상) ---
    - mock DB rows: 2024-01 3건, 2024-02 3건, 2024-03 3건
    - 기준월(2024-01) index=100
    - 이후 월은 중위가 비율에 따라 index 계산
    - 반환 배열의 각 요소에 month, index, medianPrice, count 존재

    --- 직거래 필터링 ---
    - deal_type='직거래'인 행은 지수 계산에서 제외됨
    - 직거래만 있는 월은 minTransactions 미달로 skip

    --- minTransactions 미달 ---
    - 거래 1건만 있는 월은 결과에서 제외
    - 모든 월이 minTransactions 미달이면 [] 반환

    --- 기준월 중위가가 0 ---
    - 모든 가격이 0이면 [] 반환 (baseMedian=0 guard)

    --- 커스텀 minTransactions ---
    - minTransactions=1로 설정하면 1건인 월도 포함
  </behavior>
  <action>
`tests/unit/cluster-index.test.ts` 생성.

vi.mock 패턴:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// DB 모듈 mock — vi.mock은 Vitest가 자동 hoist
vi.mock('@/lib/db/client', () => ({
  getPool: vi.fn(),
}));

import { computeClusterIndex } from '@/lib/cluster-index';
import { getPool } from '@/lib/db/client';
```

각 테스트에서 mockQuery를 설정:
```typescript
beforeEach(() => {
  vi.clearAllMocks();
});

// 헬퍼: mock pool 설정
function mockPoolQuery(rows: Array<{trade_date: string; trade_price: number|string; floor: number|string; deal_type: string|null}>) {
  const mockQuery = vi.fn().mockResolvedValue({ rows });
  vi.mocked(getPool).mockReturnValue({ query: mockQuery } as unknown as ReturnType<typeof getPool>);
  return mockQuery;
}
```

주의사항:
- `vi.mock` factory 내에서 외부 변수 참조 금지 (hoisting 때문)
- `vi.mocked(getPool)` 사용하여 TypeScript strict 타입 호환
- trade_price, floor이 string으로 올 수 있음 (DB 반환값) — Number() 변환 테스트 포함
- getPool 호출 여부로 빈 배열 케이스에서 DB 미호출 검증
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npm test 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/cluster-index.test.ts가 존재
    - vi.mock으로 getPool이 mock됨 (실제 DB 연결 없음)
    - 빈 regionCodes -> 빈 배열 반환 + DB 미호출 검증
    - 정상 3개월 데이터 -> 기준월 index=100, 이후 월 비례 계산 검증
    - 직거래 행 필터링 검증
    - minTransactions 미달 월 skip 검증
    - 전체 미달 시 빈 배열 반환 검증
    - `npm test` 실행 시 전체 통과 (exit code 0)
  </acceptance_criteria>
  <done>computeClusterIndex의 모든 주요 분기와 엣지 케이스가 유닛 테스트로 커버되고 통과한다</done>
</task>

</tasks>

<verification>
```bash
# 전체 테스트 통과 (price-normalization + cluster-index)
npm test

# cluster-index 테스트만 실행
npx vitest run tests/unit/cluster-index.test.ts
```
</verification>

<success_criteria>
1. computeClusterIndex에 대한 유닛 테스트가 최소 5개 케이스 존재
2. vi.mock으로 DB가 격리되어 실제 DB 연결 없이 테스트 실행
3. `npm test` 실행 시 price-normalization + cluster-index 테스트 모두 통과
4. CI에서 테스트 실패 시 non-zero exit code 반환
</success_criteria>

<output>
After completion, create `.planning/phases/16-test-infrastructure/16-02-SUMMARY.md`
</output>
