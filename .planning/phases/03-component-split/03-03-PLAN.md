---
phase: "03"
plan: "03"
type: autonomous
wave: 1
depends_on: []
requirements: [CLN-06]
---

# Plan 03-03: src/app/page.tsx 컴포넌트 분할

## Objective

src/app/page.tsx (663줄)를 섹션별 서브 컴포넌트로 분리하여 300줄 이하로 줄인다.

## Context

- src/app/page.tsx: 663줄, 홈페이지 메인 파일
- 현재 파일 내부에 HeroSection, StatsBar, RateBar, SearchCTA, PopularComplexes, SidebarRateCard, StatBarItem, QuickLinkCard 등이 혼재
- src/components/home/ 에 RankingTabs.tsx 만 존재
- Phase 2에서 formatPrice를 @/lib/format에서 import하도록 정리 완료

## Tasks

### Task 1: HeroSection 컴포넌트 추출
type: auto

src/components/home/HeroSection.tsx 생성:
- heroTx, heroHigh, today props 수신
- 드라마틱 헤드라인 + 서브라인 렌더링
- formatPrice import from @/lib/format

### Task 2: StatsBar 컴포넌트 추출
type: auto

src/components/home/StatsBar.tsx 생성:
- StatBarItem 인라인 컴포넌트 포함
- totalTxns, totalComplexes, dropCount, highCount props 수신

### Task 3: RateBar + SidebarRateCard 컴포넌트 추출
type: auto

src/components/home/RateBar.tsx 생성 (상단 가로 금리 바):
- sortedRates props 수신
- RATE_LABELS import from @/lib/format

src/components/home/SidebarRateCard.tsx 생성 (사이드바 금리 카드):
- sortedRates props 수신

### Task 4: SearchCTA + QuickLinkCard + PopularComplexes 추출
type: auto

src/components/home/SearchCTA.tsx 생성 (바이럴 CTA 배너)
src/components/home/QuickLinks.tsx 생성 (QuickLinkCard 포함)
src/components/home/PopularComplexes.tsx 생성 (인기 단지 TOP)

### Task 5: page.tsx 리팩토링
type: auto

page.tsx를 서브 컴포넌트 import 방식으로 교체:
- 데이터 페칭 로직은 유지
- JSX는 컴포넌트 호출로 대체
- 300줄 이하 달성
- pnpm build 검증

## Verification

- wc -l src/app/page.tsx → 300줄 이하
- pnpm build 성공
- 각 서브 컴포넌트 파일 생성 확인

## Success Criteria

- src/app/page.tsx 300줄 이하
- pnpm build 성공
- 기능 변경 없음 (props 동일 데이터 전달)
