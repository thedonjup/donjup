---
phase: 02-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/map/KakaoMap.tsx
  - src/lib/api/instagram.ts
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03]

must_haves:
  truths:
    - "formatPrice in KakaoMap.tsx is imported from @/lib/format, not locally defined"
    - "Only one Instagram client module exists (src/lib/instagram/client.ts)"
    - "postgres and @neondatabase/serverless are not in package.json"
  artifacts:
    - path: "src/components/map/KakaoMap.tsx"
      provides: "KakaoMap component using shared formatPrice"
      contains: "import.*formatPrice.*from.*@/lib/format"
    - path: "src/lib/instagram/client.ts"
      provides: "Single Instagram API client with rate limiting"
  key_links:
    - from: "src/components/map/KakaoMap.tsx"
      to: "src/lib/format.ts"
      via: "import { formatPrice }"
      pattern: "import.*formatPrice.*@/lib/format"
---

<objective>
Remove duplicate code and unused packages: deduplicate formatPrice in KakaoMap.tsx, delete unused Instagram client, remove unused PostgreSQL packages.

Purpose: Eliminate dead code and duplications that create maintenance burden.
Output: Cleaner dependency tree and single-source utility functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@src/components/map/KakaoMap.tsx
@src/lib/format.ts
@src/lib/api/instagram.ts
@src/lib/instagram/client.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate formatPrice and unused Instagram client</name>
  <files>src/components/map/KakaoMap.tsx, src/lib/api/instagram.ts</files>
  <read_first>
    - src/components/map/KakaoMap.tsx (line 41 — local formatPrice definition)
    - src/lib/format.ts (line 2 — canonical formatPrice export)
    - src/lib/api/instagram.ts (entire file — the unused duplicate)
  </read_first>
  <action>
    **CLN-01 (formatPrice dedup):**
    1. In `src/components/map/KakaoMap.tsx`:
       - Add `import { formatPrice } from "@/lib/format";` to the existing imports at the top of the file
       - Remove the local `function formatPrice(price: number): string { ... }` definition at line 41 (the function body is ~7 lines)
       - All existing usages of formatPrice in the file (lines 173, 256, 378, 617) remain unchanged since they already call `formatPrice(...)` by name

    **CLN-02 (Instagram client consolidation):**
    1. Delete the file `src/lib/api/instagram.ts` entirely
       - This file is NOT imported anywhere in the codebase (confirmed by grep)
       - The active Instagram client is `src/lib/instagram/client.ts` (used by `src/app/api/cron/post-instagram/route.ts`)
       - The active client has rate limiting, carousel support, and quota checking — strictly superior
    2. If `src/lib/api/` directory becomes empty after deletion, remove the directory too
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -n "function formatPrice" src/components/map/KakaoMap.tsx && echo "FAIL: local formatPrice still exists" || echo "PASS: no local formatPrice" && grep -n "import.*formatPrice.*@/lib/format" src/components/map/KakaoMap.tsx && echo "PASS: import exists" || echo "FAIL: import missing" && test ! -f src/lib/api/instagram.ts && echo "PASS: unused instagram file deleted" || echo "FAIL: file still exists"</automated>
  </verify>
  <acceptance_criteria>
    - grep "function formatPrice" src/components/map/KakaoMap.tsx returns 0 results
    - grep "import.*formatPrice.*@/lib/format" src/components/map/KakaoMap.tsx returns 1 result
    - src/lib/api/instagram.ts does not exist
    - src/lib/instagram/client.ts still exists unchanged
  </acceptance_criteria>
  <done>KakaoMap uses shared formatPrice from @/lib/format. Unused Instagram client file deleted. Active client with rate limiting preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused PostgreSQL packages and verify build</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (check current postgres and @neondatabase/serverless entries)
  </read_first>
  <action>
    **CLN-03 (unused DB packages):**
    1. Run `pnpm remove postgres @neondatabase/serverless` to uninstall both packages
       - `postgres` (^3.4.8) — not imported anywhere in src/
       - `@neondatabase/serverless` (^1.0.2) — not imported anywhere in src/
       - The active DB driver is `pg` (8.20.0) which remains
    2. Run `pnpm build` to verify no breakage
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && ! grep -q '"postgres"' package.json && echo "PASS: postgres removed" || echo "FAIL: postgres still in package.json" && ! grep -q '"@neondatabase/serverless"' package.json && echo "PASS: neon removed" || echo "FAIL: neon still in package.json" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep '"postgres"' package.json returns 0 results
    - grep '"@neondatabase/serverless"' package.json returns 0 results
    - grep '"pg"' package.json returns 1 result (active driver preserved)
    - pnpm build succeeds with exit code 0
  </acceptance_criteria>
  <done>Unused postgres and @neondatabase/serverless packages removed. Build passes. Only pg driver remains.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `grep -rn "function formatPrice" src/components/map/KakaoMap.tsx` returns nothing
- `ls src/lib/api/instagram.ts` fails (file gone)
- `grep '"postgres"\|"@neondatabase"' package.json` returns nothing
</verification>

<success_criteria>
- formatPrice in KakaoMap.tsx imported from @/lib/format, no local definition
- Only src/lib/instagram/client.ts exists for Instagram API
- postgres and @neondatabase/serverless removed from package.json
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-cleanup/02-01-SUMMARY.md`
</output>
