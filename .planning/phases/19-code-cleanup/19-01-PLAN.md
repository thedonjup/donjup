---
phase: 19-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/analytics/pageview/route.ts
  - src/app/api/bank-rates/route.ts
  - src/app/api/cron/fetch-bank-rates/route.ts
  - src/app/api/cron/fetch-transactions/route.ts
  - src/app/api/cron/generate-cardnews/route.ts
  - src/app/api/cron/post-instagram/route.ts
  - src/app/api/cron/validate-data/route.ts
  - src/app/api/geocode/route.ts
  - src/app/apt/[region]/[slug]/opengraph-image.tsx
  - src/app/apt/[region]/[slug]/page.tsx
  - src/app/daily/archive/page.tsx
  - src/app/dam/comments/page.tsx
  - src/app/market/[sido]/[sigungu]/page.tsx
  - src/app/market/page.tsx
  - src/app/page.tsx
  - src/app/rate/calculator/components/LoanCalculatorTab.tsx
  - src/app/rate/page.tsx
  - src/app/search/page.tsx
  - src/app/themes/[slug]/page.tsx
  - src/app/themes/page.tsx
  - src/app/today/page.tsx
  - src/components/charts/PriceHistoryChartWrapper.tsx
  - src/components/map/MobileBottomSheet.tsx
  - src/lib/db/schema/daily-reports.ts
  - src/lib/db/schema/push-subscriptions.ts
  - src/proxy.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "npx eslint src/ reports 0 no-unused-vars warnings"
    - "as any casts in production code are fewer than 5 (target: 0)"
    - "No legacy DB patterns (createClient, createRentServiceClient, getPool export) exist"
  artifacts:
    - path: "src/components/charts/PriceHistoryChartWrapper.tsx"
      provides: "Type-safe dynamic import wrapper without as any"
    - path: "src/app/api/analytics/pageview/route.ts"
      provides: "Clean imports with no unused variables"
  key_links:
    - from: "src/components/charts/PriceHistoryChartWrapper.tsx"
      to: "src/components/charts/PriceHistoryChart"
      via: "dynamic import with proper typing"
      pattern: "dynamic.*import.*PriceHistoryChart"
---

<objective>
Remove all unused imports/variables and the single `as any` cast from production code.

Purpose: Achieve CLEAN-01 (as any < 5), CLEAN-02 (unused imports 0), and verify CLEAN-03 (legacy DB patterns already removed in Phase 18).
Output: Clean ESLint run with 0 no-unused-vars warnings and 0 no-explicit-any errors in production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all unused imports and variables across production code</name>
  <read_first>
    - src/app/api/analytics/pageview/route.ts
    - src/app/api/bank-rates/route.ts
    - src/app/api/cron/fetch-bank-rates/route.ts
    - src/app/api/cron/fetch-transactions/route.ts
    - src/app/api/cron/generate-cardnews/route.ts
    - src/app/api/cron/post-instagram/route.ts
    - src/app/api/cron/validate-data/route.ts
    - src/app/api/geocode/route.ts
    - src/app/apt/[region]/[slug]/opengraph-image.tsx
    - src/app/apt/[region]/[slug]/page.tsx
    - src/app/daily/archive/page.tsx
    - src/app/dam/comments/page.tsx
    - src/app/market/[sido]/[sigungu]/page.tsx
    - src/app/market/page.tsx
    - src/app/page.tsx
    - src/app/rate/calculator/components/LoanCalculatorTab.tsx
    - src/app/rate/page.tsx
    - src/app/search/page.tsx
    - src/app/themes/[slug]/page.tsx
    - src/app/themes/page.tsx
    - src/app/today/page.tsx
    - src/components/map/MobileBottomSheet.tsx
    - src/lib/db/schema/daily-reports.ts
    - src/lib/db/schema/push-subscriptions.ts
    - src/proxy.ts
  </read_first>
  <files>
    src/app/api/analytics/pageview/route.ts,
    src/app/api/bank-rates/route.ts,
    src/app/api/cron/fetch-bank-rates/route.ts,
    src/app/api/cron/fetch-transactions/route.ts,
    src/app/api/cron/generate-cardnews/route.ts,
    src/app/api/cron/post-instagram/route.ts,
    src/app/api/cron/validate-data/route.ts,
    src/app/api/geocode/route.ts,
    src/app/apt/[region]/[slug]/opengraph-image.tsx,
    src/app/apt/[region]/[slug]/page.tsx,
    src/app/daily/archive/page.tsx,
    src/app/dam/comments/page.tsx,
    src/app/market/[sido]/[sigungu]/page.tsx,
    src/app/market/page.tsx,
    src/app/page.tsx,
    src/app/rate/calculator/components/LoanCalculatorTab.tsx,
    src/app/rate/page.tsx,
    src/app/search/page.tsx,
    src/app/themes/[slug]/page.tsx,
    src/app/themes/page.tsx,
    src/app/today/page.tsx,
    src/components/map/MobileBottomSheet.tsx,
    src/lib/db/schema/daily-reports.ts,
    src/lib/db/schema/push-subscriptions.ts,
    src/proxy.ts
  </files>
  <action>
    Remove all unused imports and variables flagged by ESLint @typescript-eslint/no-unused-vars.

    Specific removals per file (from ESLint scan 2026-03-28):

    1. src/app/api/analytics/pageview/route.ts — remove `eq`, `and` from drizzle import; remove unused destructured vars `regionCode`, `complexId`, `utmSource`, `utmMedium`, `utmCampaign` (replace with `_` prefix or remove from destructuring)
    2. src/app/api/bank-rates/route.ts — remove `like` from drizzle import
    3. src/app/api/cron/fetch-bank-rates/route.ts — remove `lt` from drizzle import
    4. src/app/api/cron/fetch-transactions/route.ts — remove `eq`, `gte` from drizzle import; remove `logger` import; remove `ParsedMultiTransaction` type import; remove unused `regionName` and `threeYearsAgoStr` variables
    5. src/app/api/cron/generate-cardnews/route.ts — remove unused `buffers` variable
    6. src/app/api/cron/post-instagram/route.ts — remove `like` from drizzle import
    7. src/app/api/cron/validate-data/route.ts — remove `not` from drizzle import; remove `logger` import
    8. src/app/api/geocode/route.ts — remove unused `err` in catch clause (use bare catch or `_err`)
    9. src/app/apt/[region]/[slug]/opengraph-image.tsx — remove `and` from drizzle import
    10. src/app/apt/[region]/[slug]/page.tsx — change `let complexRows` to `const complexRows` (prefer-const)
    11. src/app/daily/archive/page.tsx — remove `DailyReport` type import
    12. src/app/dam/comments/page.tsx — remove `collection` from schema import
    13. src/app/market/[sido]/[sigungu]/page.tsx — remove `getSidoForCode` from regions import
    14. src/app/market/page.tsx — remove `aptComplexes` from schema import
    15. src/app/page.tsx — remove `isNull` from drizzle import
    16. src/app/rate/calculator/components/LoanCalculatorTab.tsx — remove `formatPrice` import
    17. src/app/rate/page.tsx — remove `like` from drizzle import
    18. src/app/search/page.tsx — remove `formatSizeWithPyeong` import; remove `REGION_HIERARCHY` import
    19. src/app/themes/[slug]/page.tsx — remove `isNotNull` from drizzle import
    20. src/app/themes/page.tsx — remove `isNotNull` from drizzle import
    21. src/app/today/page.tsx — remove `and` from drizzle import
    22. src/components/map/MobileBottomSheet.tsx — remove `activeId` from props/destructuring
    23. src/lib/db/schema/daily-reports.ts — remove `unique` from drizzle import
    24. src/lib/db/schema/push-subscriptions.ts — remove `unique` from drizzle import
    25. src/proxy.ts — prefix `request` param with `_` or remove if unused

    IMPORTANT: For each file, only remove the specific unused identifier. Do NOT remove identifiers that are actually used elsewhere in the same file. Read the full file before editing.

    For drizzle-orm imports like `import { eq, and, desc } from "drizzle-orm"`, remove only the unused identifiers from the named import list — keep the ones that are used.

    For destructured variables that are unused but part of a larger destructure, either prefix with `_` or remove them. If removing would change the destructure position of other vars, use `_` prefix instead.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx eslint src/ 2>&1 | grep -c "no-unused-vars" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ 2>&1 | grep "no-unused-vars"` returns 0 lines
    - `npx eslint src/ 2>&1 | grep "prefer-const"` returns 0 lines
    - `npx tsc --noEmit` passes with no errors
    - All 25 files edited, each with only the targeted unused identifier removed
  </acceptance_criteria>
  <done>All @typescript-eslint/no-unused-vars warnings and prefer-const errors are resolved across production code. No regressions in TypeScript compilation.</done>
</task>

<task type="auto">
  <name>Task 2: Fix as-any cast in PriceHistoryChartWrapper and verify CLEAN-03</name>
  <read_first>
    - src/components/charts/PriceHistoryChartWrapper.tsx
    - src/components/charts/PriceHistoryChart.tsx
    - src/lib/db/index.ts
  </read_first>
  <files>src/components/charts/PriceHistoryChartWrapper.tsx</files>
  <action>
    CLEAN-01: Fix the single `as any` cast in PriceHistoryChartWrapper.tsx (line 22).

    The wrapper passes props to a dynamically imported PriceHistoryChart component. The `as any` exists because `dynamic()` returns `ComponentType<{}>` by default.

    Fix approach: Type the dynamic import properly. Read PriceHistoryChart.tsx to find its props type (likely exported as Props or PriceHistoryChartProps). Then either:
    - Option A: Use `dynamic<PropsType>(() => import(...))` to type the dynamic component
    - Option B: Cast to the correct props type instead of `any`

    After fix, remove the `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment on line 21.

    CLEAN-03 verification: Confirm that `src/lib/db/index.ts` has `getPool` as a non-exported internal function (already true). Grep codebase for any remaining `createClient`, `createRentServiceClient`, or exported `getPool` patterns — should be 0.

    Run final comprehensive ESLint check and TypeScript check to confirm all CLEAN requirements met.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx eslint src/ --max-warnings 50 2>&1 | grep -c "no-explicit-any" | xargs -I{} test {} -eq 0 && grep -r "as any" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test." | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "as any" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test."` returns 0 lines (or fewer than 5 if edge cases)
    - No `eslint-disable.*no-explicit-any` comments remain in PriceHistoryChartWrapper.tsx
    - `npx tsc --noEmit` passes
    - `grep -rn "createClient\|createRentServiceClient" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v "import.*drizzle"` returns 0 lines
    - `grep -n "export.*getPool\|export function getPool" src/lib/db/index.ts` returns 0 lines
  </acceptance_criteria>
  <done>Production code has 0 `as any` casts (CLEAN-01 exceeded). Legacy DB patterns confirmed absent (CLEAN-03). Combined with Task 1, all CLEAN requirements are met.</done>
</task>

</tasks>

<verification>
Run all three verification commands to confirm phase success:

1. `npx eslint src/ 2>&1 | grep "no-unused-vars"` — must return 0 lines (CLEAN-02)
2. `grep -r "as any" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test."` — must return fewer than 5 lines (CLEAN-01)
3. `grep -rn "createClient\|createRentServiceClient" src/ --include="*.ts" | grep -v node_modules` — must return 0 lines (CLEAN-03)
4. `npx tsc --noEmit` — must pass with 0 errors
5. `npm run build` — must succeed
</verification>

<success_criteria>
- ESLint no-unused-vars warnings: 0
- Production `as any` casts: 0 (target was < 5)
- Legacy DB patterns (createClient/createRentServiceClient/exported getPool): 0
- TypeScript compilation: clean
- Build: passes
</success_criteria>

<output>
After completion, create `.planning/phases/19-code-cleanup/19-01-SUMMARY.md`
</output>
