---
phase: 05-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate-pg-trgm.sql
  - src/lib/db/client.ts
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Search queries on apt_name, region_name, dong_name use GIN index instead of sequential scan"
    - "DB connection pool handles concurrent cron jobs without exhaustion"
  artifacts:
    - path: "scripts/migrate-pg-trgm.sql"
      provides: "SQL migration for pg_trgm extension and GIN indexes"
      contains: "CREATE INDEX"
    - path: "src/lib/db/client.ts"
      provides: "Updated pool configuration"
      contains: "max:"
  key_links:
    - from: "src/app/api/search/route.ts"
      to: "apt_complexes GIN index"
      via: "ILIKE queries now accelerated by pg_trgm"
      pattern: "ILIKE"
---

<objective>
Enable pg_trgm GIN indexes for search performance and fix DB connection pool sizing.

Purpose: Search queries currently do sequential scans on ILIKE. Cron jobs interleave with 5-min gaps sharing a pool of 5 connections, risking exhaustion.
Output: SQL migration script + updated pool config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/db/client.ts
@src/app/api/search/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pg_trgm migration script and update pool config</name>
  <files>scripts/migrate-pg-trgm.sql, src/lib/db/client.ts</files>
  <read_first>src/lib/db/client.ts, src/app/api/search/route.ts</read_first>
  <action>
1. Create `scripts/migrate-pg-trgm.sql` with:
   - `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
   - `CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_apt_complexes_apt_name_trgm ON apt_complexes USING GIN (apt_name gin_trgm_ops);`
   - `CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_apt_complexes_region_name_trgm ON apt_complexes USING GIN (region_name gin_trgm_ops);`
   - `CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_apt_complexes_dong_name_trgm ON apt_complexes USING GIN (dong_name gin_trgm_ops);`
   - Add a comment at top: "-- Run against production DB: psql $DATABASE_URL -f scripts/migrate-pg-trgm.sql"
   - Add a verification query at the bottom (commented out): `-- EXPLAIN ANALYZE SELECT * FROM apt_complexes WHERE apt_name ILIKE '%래미안%';`

2. In `src/lib/db/client.ts`, update the Pool config:
   - Change `max: 5` to `max: 10` — reasoning: 22 cron jobs can overlap (transactions batch 0-4 interleave with rents batch 0-4), and user traffic runs concurrently. 10 is conservative but safe for Neon/CockroachDB free tier.
   - Change `idleTimeoutMillis: 30_000` to `idleTimeoutMillis: 20_000` — faster release of idle connections back to pool.
   - Keep `connectionTimeoutMillis: 10_000` unchanged.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && cat scripts/migrate-pg-trgm.sql | grep -c "CREATE INDEX" && grep "max:" src/lib/db/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/migrate-pg-trgm.sql contains CREATE EXTENSION pg_trgm and 3 GIN indexes
    - src/lib/db/client.ts has max: 10 and idleTimeoutMillis: 20_000
  </acceptance_criteria>
  <done>Migration script ready to run against production. Pool sized for concurrent cron + user traffic.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run pg_trgm migration against production DB</name>
  <what-built>SQL migration script at scripts/migrate-pg-trgm.sql</what-built>
  <how-to-verify>
    1. Run: `psql $DATABASE_URL -f scripts/migrate-pg-trgm.sql`
       - If CockroachDB, pg_trgm may not be available. In that case, skip EXTENSION creation and try creating GIN indexes with standard ops instead.
       - If Neon PostgreSQL, pg_trgm should work natively.
    2. Verify indexes exist: `psql $DATABASE_URL -c "\di idx_apt_complexes_*"`
    3. Test search performance: `psql $DATABASE_URL -c "EXPLAIN ANALYZE SELECT * FROM apt_complexes WHERE apt_name ILIKE '%래미안%';"`
       - Should show "Bitmap Index Scan" using the trgm index, NOT "Seq Scan"
  </how-to-verify>
  <resume-signal>Confirm migration ran successfully, or report any errors (e.g., pg_trgm not supported on your DB provider)</resume-signal>
</task>

</tasks>

<verification>
- `grep "max: 10" src/lib/db/client.ts` returns a match
- `scripts/migrate-pg-trgm.sql` exists with 3 CREATE INDEX statements
- After migration: EXPLAIN on ILIKE query shows index scan
</verification>

<success_criteria>
- pg_trgm GIN indexes created on apt_name, region_name, dong_name
- Connection pool max increased to 10 with faster idle timeout
- Search queries benefit from trigram indexes (verified via EXPLAIN)
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance/05-01-SUMMARY.md`
</output>
