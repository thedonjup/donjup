---
phase: 05-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - src/proxy.ts
autonomous: true
requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "No two cron jobs run within the same minute"
    - "Transaction and rent batches are fully separated in time"
    - "In-memory rate limiter is removed (useless in serverless)"
  artifacts:
    - path: "vercel.json"
      provides: "Staggered cron schedules with minimum 10-min gaps between heavy DB jobs"
      contains: "crons"
    - path: "src/proxy.ts"
      provides: "Proxy file with rate limiter removed, just passes through"
      contains: "NextResponse.next"
  key_links:
    - from: "vercel.json"
      to: "src/app/api/cron/*"
      via: "Cron schedule triggers"
      pattern: "schedule"
---

<objective>
Stagger cron schedules to prevent DB connection exhaustion and remove useless in-memory rate limiter.

Purpose: Current schedules interleave transaction and rent fetches with 5-min gaps, risking concurrent DB pool exhaustion. The in-memory rate limiter resets on every serverless cold start, providing no real protection.
Output: Staggered vercel.json crons + simplified proxy.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vercel.json
@src/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stagger cron schedules in vercel.json</name>
  <files>vercel.json</files>
  <read_first>vercel.json</read_first>
  <action>
Reorganize cron schedules to ensure:
- Minimum 10-minute gap between any two DB-heavy jobs
- Transaction batches run sequentially (not interleaved with rents)
- Rent batches run AFTER all transaction batches complete
- Light jobs (cardnews, instagram, push) can stay in their current slots

New schedule (all UTC):

```
# --- Afternoon: light/weekly jobs ---
10:00 Mon  fetch-reb-index
10:30 Mon  fetch-bank-rates          (was 10:00 Mon — moved to avoid overlap)
15:00      fetch-transactions type=2 batch=0
16:00      fetch-transactions type=3 batch=0
17:00      geocode-complexes

# --- Evening: content generation ---
19:00      generate-cardnews
19:30      post-instagram

# --- Night: transaction batches (sequential, 10-min gaps) ---
20:00      fetch-transactions batch=0
20:10      fetch-transactions batch=1
20:20      fetch-transactions batch=2
20:30      fetch-transactions batch=3
20:40      fetch-transactions batch=4

# --- Rent batches START AFTER transactions done (21:00+) ---
21:00      fetch-rents batch=0        (was 20:05 — moved)
21:10      fetch-rents batch=1        (was 20:15 — moved)
21:20      fetch-rents batch=2        (was 20:25 — moved)
21:30      fetch-rents batch=3        (was 20:35 — moved)
21:40      fetch-rents batch=4        (was 20:45 — moved)

# --- Post-processing (after all data fetched) ---
21:50      enrich-complexes           (unchanged, safe — rents batch=4 should finish by then)
22:10      fetch-rates                (was 22:00 — 10-min gap after enrich)
22:30      validate-data              (was 22:50 — moved earlier, 20-min gap)
23:00      generate-report            (unchanged)
23:10      send-push                  (was 23:05 — 10-min gap)
23:30      generate-seeding           (unchanged)
```

Write the vercel.json with the above schedule. Keep the exact same paths, only change schedule values.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && node -e "const v=require('./vercel.json'); const scheds=v.crons.map(c=>c.schedule); const minutes=scheds.map(s=>{const p=s.split(' '); return p[1]+':'+p[0]}); console.log('Cron count:', v.crons.length); console.log('Schedules:', minutes.sort().join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json has exactly 22 cron entries (same as before)
    - No fetch-rents job runs between 20:00-20:50 (moved to 21:00+)
    - fetch-reb-index and fetch-bank-rates do NOT share the same minute
    - All cron paths are unchanged from original
  </acceptance_criteria>
  <done>Cron schedules staggered with transactions and rents fully separated. Minimum 10-min gaps between DB-heavy jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Remove in-memory rate limiter from proxy.ts</name>
  <files>src/proxy.ts</files>
  <read_first>src/proxy.ts</read_first>
  <action>
Replace the entire content of src/proxy.ts with a minimal pass-through that documents WHY the rate limiter was removed:

```typescript
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

// Rate limiting removed — in-memory Map resets on every serverless cold start,
// providing no real protection. Use Vercel WAF or Vercel Firewall Rules
// for production rate limiting instead.
// See: https://vercel.com/docs/security/vercel-waf

export function proxy(request: NextRequest) {
  return NextResponse.next();
}

export const config = {
  matcher: "/api/:path*",
};
```

Check if proxy() is imported/called anywhere. If it is wired into middleware or next.config, keep the export signature intact (function name, config).
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -c "rateLimitMap\|new Map" src/proxy.ts; echo "---"; grep "proxy" src/proxy.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/proxy.ts contains NO Map, NO rateLimitMap, NO WINDOW_MS, NO MAX_REQUESTS
    - proxy function still exported with same signature (request: NextRequest) => NextResponse
    - config.matcher still exports "/api/:path*"
    - Comment explains why rate limiter was removed and suggests Vercel WAF
  </acceptance_criteria>
  <done>In-memory rate limiter removed. File contains pass-through with documentation comment suggesting Vercel WAF for real rate limiting.</done>
</task>

</tasks>

<verification>
- `grep -c "fetch-rents" vercel.json` returns 5
- `grep "20:05\|20:15\|20:25\|20:35\|20:45" vercel.json` returns 0 matches (old interleaved times gone)
- `grep "rateLimitMap" src/proxy.ts` returns 0 matches
- `pnpm build` succeeds
</verification>

<success_criteria>
- Transaction batches (20:00-20:40) and rent batches (21:00-21:40) fully separated
- No two DB-heavy cron jobs share the same minute
- In-memory rate limiter removed with clear documentation of why
- Build passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance/05-02-SUMMARY.md`
</output>
