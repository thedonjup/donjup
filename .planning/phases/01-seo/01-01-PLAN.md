---
phase: 01-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/dam/layout.tsx
  - src/app/dam/cron/page.tsx
  - src/app/dam/comments/page.tsx
  - src/app/dam/content/page.tsx
  - src/app/dam/data/page.tsx
  - src/app/dam/settings/page.tsx
  - src/app/dam/users/page.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02

must_haves:
  truths:
    - "모든 공개 페이지의 canonical URL이 자기 자신을 가리킨다"
    - "compare, profile, dam 페이지에 고유 title이 존재한다"
    - "dam 관리자 하위 페이지가 개별 title을 가진다"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "홈페이지 canonical URL"
      contains: "alternates"
    - path: "src/app/layout.tsx"
      provides: "루트 레이아웃 canonical (/) 설정"
      contains: "canonical"
  key_links:
    - from: "src/app/page.tsx"
      to: "https://donjup.com/"
      via: "alternates.canonical metadata"
      pattern: 'canonical.*"/"'
---

<objective>
모든 페이지에 올바른 canonical URL을 설정하고, 고유 title이 누락된 페이지에 title을 추가한다.

Purpose: 검색엔진이 각 페이지를 개별적으로 인식하도록 canonical 중복 문제를 해결한다.
Output: 수정된 metadata가 포함된 page/layout 파일들
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/app/layout.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 페이지 canonical URL 점검 및 홈페이지 canonical 추가</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>src/app/page.tsx, src/app/layout.tsx</read_first>
  <action>
0. 서브 페이지 canonical 현황을 일괄 확인한다:
   `grep -rn "canonical" src/app/` 실행하여 모든 페이지의 canonical 설정 현황을 파악한다.
   누락된 공개 페이지(noindex가 아닌 페이지)가 있으면 해당 페이지에도 canonical을 추가한다.

1. src/app/page.tsx에 metadata export를 추가한다. 현재 이 파일에는 metadata export가 없다. 파일 상단(import 이후)에 다음을 추가:

```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  alternates: { canonical: "/" },
};
```

만약 이미 Metadata import가 있다면 중복하지 않는다. 기존에 metadata export가 없으므로 새로 추가한다.

2. src/app/layout.tsx의 metadata에서 `alternates: {},`를 `alternates: { canonical: "/" },`로 변경한다. 이렇게 하면 하위 페이지에서 자체 canonical을 지정하지 않은 경우에도 최소한 루트를 가리키게 된다. 하위 페이지들은 각자 자신의 canonical로 오버라이드하고 있으므로 문제없다.

3. Step 0에서 canonical이 누락된 공개 페이지를 발견한 경우, 해당 페이지의 metadata에 alternates.canonical을 추가한다. files_modified에 없는 파일도 필요 시 수정한다.

SEO-01 요구사항: 모든 페이지의 canonical URL이 해당 페이지의 실제 URL을 가리킨다.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -rn "canonical" src/app/ && echo "---" && grep -n "canonical" src/app/page.tsx src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
- src/app/page.tsx에 `alternates: { canonical: "/" }` 포함
- src/app/layout.tsx에 `alternates: { canonical: "/" }` 포함 (빈 객체 아님)
- 모든 공개 페이지에 canonical이 설정되어 있음 (누락 없음)
- pnpm build 성공
  </acceptance_criteria>
  <done>홈페이지와 루트 레이아웃에 canonical URL이 설정되고, 모든 서브 페이지의 canonical 현황이 확인/보완됨</done>
</task>

<task type="auto">
  <name>Task 2: dam 관리자 하위 페이지에 개별 title 추가</name>
  <files>src/app/dam/layout.tsx, src/app/dam/cron/page.tsx, src/app/dam/comments/page.tsx, src/app/dam/content/page.tsx, src/app/dam/data/page.tsx, src/app/dam/settings/page.tsx, src/app/dam/users/page.tsx</files>
  <read_first>src/app/dam/layout.tsx, src/app/dam/cron/page.tsx, src/app/dam/comments/page.tsx, src/app/dam/content/page.tsx, src/app/dam/data/page.tsx, src/app/dam/settings/page.tsx, src/app/dam/users/page.tsx</read_first>
  <action>
dam 하위 페이지들은 "use client" 컴포넌트라 metadata export가 불가능하다. dam/layout.tsx에서 title template을 활용하면 된다.

1. src/app/dam/layout.tsx의 metadata를 다음으로 변경:
```typescript
export const metadata: Metadata = {
  title: {
    default: "관리자",
    template: "%s | 관리자",
  },
  robots: { index: false, follow: false },
};
```

2. dam 하위 페이지들은 "use client"이므로 metadata export 불가. 대신 각 페이지 폴더에 별도 layout.tsx 또는 metadata route를 만들 수 없다면, 현재 "관리자" title 상속으로 충분하다. dam 페이지는 noindex이므로 SEO 영향 없음.

다만, SEO-02 요구사항에서 명시한 compare, profile, dam은 이미 고유 title이 있다:
- compare: "아파트 비교"
- profile: "내 프로필"
- dam: "관리자"

이 task에서는 dam/layout.tsx의 title을 template 구조로 변환하여 하위 페이지 구분을 개선한다.

SEO-02 요구사항: compare, profile, dam 페이지에 고유 title이 설정된다.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -n "title" src/app/dam/layout.tsx && grep -n "title" src/app/compare/layout.tsx src/app/profile/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
- src/app/dam/layout.tsx에 title이 template 구조로 설정됨
- compare layout에 "아파트 비교" title 존재 (기존 확인)
- profile layout에 "내 프로필" title 존재 (기존 확인)
- pnpm build 성공
  </acceptance_criteria>
  <done>compare, profile, dam 페이지에 고유 title 확인/설정 완료</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- `grep -rn "canonical" src/app/page.tsx` -> canonical: "/" 존재
- `grep -rn "canonical" src/app/layout.tsx` -> canonical 존재
- `grep -rn "canonical" src/app/` -> 모든 공개 페이지에 canonical 존재
- `grep -rn "title" src/app/dam/layout.tsx src/app/compare/layout.tsx src/app/profile/layout.tsx` -> 각 페이지 고유 title 존재
</verification>

<success_criteria>
- 모든 공개 페이지에 canonical URL이 자기 자신을 가리킴
- compare("아파트 비교"), profile("내 프로필"), dam("관리자") 고유 title 존재
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo/01-01-SUMMARY.md`
</output>
