---
phase: 01-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/map/page.tsx
autonomous: true
requirements:
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "지도 페이지 SSR HTML에 실거래 데이터 요약 텍스트가 포함된다"
    - "/map 페이지에서 빈 상태 메시지가 보이지 않는다"
    - "today, new-highs, market, rate 페이지에 전용 OG Image가 존재한다"
  artifacts:
    - path: "src/app/map/page.tsx"
      provides: "서버 렌더링된 거래 요약 콘텐츠"
      contains: "mapTransactions"
    - path: "src/app/today/opengraph-image.tsx"
      provides: "오늘의 거래 OG 이미지"
      contains: "ImageResponse"
    - path: "src/app/new-highs/opengraph-image.tsx"
      provides: "신고가 OG 이미지"
      contains: "ImageResponse"
    - path: "src/app/market/opengraph-image.tsx"
      provides: "시장 현황 OG 이미지"
      contains: "ImageResponse"
    - path: "src/app/rate/opengraph-image.tsx"
      provides: "금리 현황 OG 이미지"
      contains: "ImageResponse"
  key_links:
    - from: "src/app/map/page.tsx"
      to: "supabase apt_transactions"
      via: "server-side data fetch"
      pattern: "supabase.*from.*apt_transactions"
---

<objective>
지도 페이지 SSR에서 크롤러가 인식할 수 있는 의미 있는 텍스트 콘텐츠를 렌더링하고, 주요 서브 페이지의 OG Image 존재를 확인한다.

Purpose: 검색엔진이 /map 페이지를 빈 페이지로 판단하지 않고, 소셜 미리보기가 정상 생성되도록 한다.
Output: 수정된 map/page.tsx, OG Image 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/app/map/page.tsx
@src/components/map/KakaoMap.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 지도 페이지에 SSR 텍스트 콘텐츠 추가</name>
  <files>src/app/map/page.tsx</files>
  <read_first>src/app/map/page.tsx, src/components/map/KakaoMap.tsx</read_first>
  <action>
현재 /map 페이지는 서버에서 데이터를 fetch하지만, 렌더링은 KakaoMap 클라이언트 컴포넌트만 반환한다. 크롤러는 "use client" 컴포넌트 내부를 실행하지 않으므로 SSR HTML이 비어 있다.

src/app/map/page.tsx의 return문을 수정하여 KakaoMap 외에 서버 렌더링되는 텍스트 섹션을 추가한다:

```tsx
export default async function MapPage() {
  // ... 기존 데이터 fetch 로직 유지 ...

  // 거래 데이터 요약 (SSR용)
  const totalCount = mapTransactions.length;
  const newHighCount = mapTransactions.filter(t => t.is_new_high).length;
  const dropCount = mapTransactions.filter(t => t.change_rate !== null && t.change_rate <= -10).length;

  // 지역별 거래 수 상위 5개
  const regionCounts: Record<string, number> = {};
  for (const t of mapTransactions) {
    regionCounts[t.region_name] = (regionCounts[t.region_name] || 0) + 1;
  }
  const topRegions = Object.entries(regionCounts)
    .sort((a, b) => b[1] - a[1])
    .slice(0, 5);

  return (
    <>
      <KakaoMap transactions={mapTransactions} />
      {/* 크롤러용 SSR 텍스트 - 시각적으로 숨김 처리 */}
      <section className="sr-only" aria-label="지도 거래 요약">
        <h1>전국 아파트 실거래가 지도</h1>
        <p>
          최근 거래 {totalCount}건을 지도에서 확인하세요.
          신고가 {newHighCount}건, 10% 이상 하락 {dropCount}건이 포함되어 있습니다.
        </p>
        {topRegions.length > 0 && (
          <>
            <h2>주요 거래 지역</h2>
            <ul>
              {topRegions.map(([region, count]) => (
                <li key={region}>{region}: {count}건</li>
              ))}
            </ul>
          </>
        )}
        <p>
          지도에서 마커를 클릭하면 아파트별 상세 실거래 정보를 확인할 수 있습니다.
          폭락(빨간색), 하락(주황색), 신고가(초록색) 마커로 구분됩니다.
        </p>
      </section>
    </>
  );
}
```

핵심 사항:
- `sr-only` 클래스로 시각적으로 숨기되 크롤러/스크린리더는 인식
- "데이터가 없습니다" 같은 빈 상태 메시지 없이, 항상 의미 있는 콘텐츠 렌더링
- transactions가 0건이어도 "최근 거래 0건" 으로 표시 (빈 상태 메시지 아님)
- aria-label로 접근성도 확보

SEO-03 요구사항: 지도 페이지 SSR에서 빈 상태 대신 유의미한 콘텐츠가 렌더링된다.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -n "sr-only\|지도 거래 요약\|전국 아파트 실거래가 지도" src/app/map/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- src/app/map/page.tsx에 sr-only 섹션이 존재
- "전국 아파트 실거래가 지도" h1 태그 포함
- "데이터가 없습니다" 문구 없음
- pnpm build 성공
  </acceptance_criteria>
  <done>/map SSR HTML에 크롤러가 인식할 수 있는 거래 요약 텍스트가 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: 주요 서브 페이지 OG Image 존재 확인 및 검증</name>
  <files></files>
  <read_first>src/app/today/opengraph-image.tsx, src/app/new-highs/opengraph-image.tsx, src/app/market/opengraph-image.tsx, src/app/rate/opengraph-image.tsx</read_first>
  <action>
SEO-04 요구사항의 4개 페이지에 전용 OG Image가 이미 존재하는지 확인한다:

1. src/app/today/opengraph-image.tsx - "오늘의 아파트 거래" 제목, 녹색 아이콘 (이미 존재)
2. src/app/new-highs/opengraph-image.tsx - "오늘의 신고가 아파트" 제목, 빨간 아이콘 (이미 존재)
3. src/app/market/opengraph-image.tsx - "전국 아파트 시장 현황" 제목 (이미 존재)
4. src/app/rate/opengraph-image.tsx - "금리 현황 대시보드" 제목, % 아이콘 (이미 존재)

모든 파일이 존재하고 각각 고유한 제목/디자인을 가지고 있으므로 추가 작업 불필요.

확인만 수행:
- 4개 파일 모두 `ImageResponse` 사용
- 4개 파일 모두 고유한 `alt` 텍스트
- 4개 파일 모두 `size: { width: 1200, height: 630 }`
- 4개 파일 모두 `contentType: "image/png"`

이미 완료된 사항이므로 파일 수정 없음. 확인 결과를 SUMMARY에 기록.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && ls -la src/app/today/opengraph-image.tsx src/app/new-highs/opengraph-image.tsx src/app/market/opengraph-image.tsx src/app/rate/opengraph-image.tsx && grep "alt =" src/app/today/opengraph-image.tsx src/app/new-highs/opengraph-image.tsx src/app/market/opengraph-image.tsx src/app/rate/opengraph-image.tsx</automated>
  </verify>
  <acceptance_criteria>
- 4개 OG Image 파일 모두 존재
- 각 파일이 고유한 alt 텍스트를 가짐
- 각 파일이 ImageResponse를 사용
  </acceptance_criteria>
  <done>today, new-highs, market, rate 페이지에 전용 OG Image 존재 확인 완료</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- `curl -s https://donjup.com/map | grep "전국 아파트 실거래가 지도"` → 결과 존재 (배포 후)
- 4개 OG Image 파일 존재 확인
- /map SSR HTML에 "데이터가 없습니다" 문구 없음
</verification>

<success_criteria>
- /map SSR HTML에 거래 요약 텍스트가 포함됨
- "데이터가 없습니다" 빈 상태 메시지 없음
- today, new-highs, market, rate에 전용 OG Image 존재
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo/01-02-SUMMARY.md`
</output>
