---
phase: 08-security
plan: 02
type: execute
wave: 1
depends_on: [07-01, 07-02]
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05]

must_haves:
  truths:
    - "DAM content API는 Firebase ID token 인증을 요구한다"
    - "push subscribe는 origin 검증 + 입력 데이터 검증을 수행한다"
    - "ADMIN_EMAILS는 서버 전용 환경변수로 이동된다"
    - "SSL rejectUnauthorized: false가 제거된다"
    - "CSP 헤더가 next.config.ts에 추가된다"
  artifacts:
    - path: "src/app/api/dam/content/route.ts"
      provides: "Firebase token auth"
      contains: "verifyIdToken"
    - path: "src/app/api/push/subscribe/route.ts"
      provides: "origin validation + input validation"
      contains: "origin"
    - path: "src/lib/admin/auth.ts"
      provides: "server-only ADMIN_EMAILS"
      contains: "ADMIN_EMAILS"
    - path: "src/lib/db/client.ts"
      provides: "SSL with rejectUnauthorized: true"
      contains: "rejectUnauthorized: true"
    - path: "next.config.ts"
      provides: "CSP header"
      contains: "Content-Security-Policy"
---

<objective>
알려진 보안 취약점을 해소한다.

Purpose: DAM 관리자 API 보호, push subscribe 악용 방지, 민감 환경변수 서버 이동, SSL 강화, CSP 헤더 추가
Output: 인증/검증이 추가된 API 엔드포인트 + 보안 강화된 설정
</objective>

<tasks>

<task type="auto">
  <name>Task 1: DAM content API 인증 추가 (SEC-02)</name>
  <files>src/app/api/dam/content/route.ts, src/app/dam/content/page.tsx</files>
  <read_first>src/app/api/dam/content/route.ts, src/app/dam/content/page.tsx, src/app/dam/users/page.tsx</read_first>
  <action>
Add Firebase ID token auth to DAM content API (GET and PATCH).
Pattern: same as /api/admin/users route + dam/users/page.tsx client pattern.

**route.ts:** Add isAdmin check after verifyIdToken for both GET and PATCH handlers.
**page.tsx:** Get Firebase ID token and pass as Authorization header.
  </action>
  <done>DAM content API requires Firebase admin token; unauthenticated requests return 401</done>
</task>

<task type="auto">
  <name>Task 2: push subscribe 보안 강화 (SEC-03)</name>
  <files>src/app/api/push/subscribe/route.ts</files>
  <read_first>src/app/api/push/subscribe/route.ts</read_first>
  <action>
Add origin validation and stricter input data validation to push subscribe endpoint.
- Validate Origin header against allowed origins (donjup.com, www.donjup.com, localhost)
- Validate endpoint URL format (must start with https://)
- Validate keys.p256dh and keys.auth are non-empty strings
  </action>
  <done>push subscribe validates origin and input data</done>
</task>

<task type="auto">
  <name>Task 3: ADMIN_EMAILS 서버 전용으로 이동 (SEC-01)</name>
  <files>src/lib/admin/auth.ts, src/app/dam/settings/page.tsx</files>
  <read_first>src/lib/admin/auth.ts, src/app/dam/settings/page.tsx</read_first>
  <action>
Change NEXT_PUBLIC_ADMIN_EMAILS to ADMIN_EMAILS in server-side code.
Note: dam/settings/page.tsx is a client component that checks this — update to use API call or remove the status check since it's not needed on client.
  </action>
  <done>No NEXT_PUBLIC_ADMIN_EMAILS usage in source code</done>
</task>

<task type="auto">
  <name>Task 4: SSL rejectUnauthorized 수정 (SEC-04)</name>
  <files>src/lib/db/client.ts</files>
  <read_first>src/lib/db/client.ts</read_first>
  <action>
Change ssl: { rejectUnauthorized: false } to ssl: { rejectUnauthorized: true } in the pg Pool config.
Neon PostgreSQL uses valid CA-signed certs so this is safe.
  </action>
  <done>rejectUnauthorized: false is removed from codebase</done>
</task>

<task type="auto">
  <name>Task 5: CSP 헤더 추가 (SEC-05)</name>
  <files>next.config.ts</files>
  <read_first>next.config.ts</read_first>
  <action>
Add Content-Security-Policy header to next.config.ts headers() config.
CSP must allow:
- Kakao SDK: dapi.kakao.com, *.dapi.kakao.com
- GA/Google: www.googletagmanager.com, www.google-analytics.com, analytics.google.com
- AdSense: pagead2.googlesyndication.com, *.googlesyndication.com, adservice.google.com
- Firebase: *.googleapis.com, *.firebaseapp.com, *.firebase.com
- Fonts: fonts.googleapis.com, fonts.gstatic.com
- Images: all (*) since apt images come from various sources
- Scripts: self + CDN domains + unsafe-inline (needed for Next.js)
- Style: self + unsafe-inline (Tailwind CSS)
  </action>
  <done>CSP header exists in next.config.ts headers()</done>
</task>

</tasks>

<verification>
- `grep "verifyIdToken" src/app/api/dam/content/route.ts` returns match
- `grep "origin" src/app/api/push/subscribe/route.ts` returns match
- `grep "NEXT_PUBLIC_ADMIN_EMAILS" src/lib/admin/auth.ts` returns 0 matches
- `grep "rejectUnauthorized" src/lib/db/client.ts` returns "true"
- `grep "Content-Security-Policy" next.config.ts` returns match
- `pnpm build` succeeds
</verification>

<success_criteria>
- DAM content API requires auth (Firebase token)
- Push subscribe has origin validation + data validation
- ADMIN_EMAILS is server-only
- SSL rejectUnauthorized is true
- CSP header exists
- pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-security/08-02-SUMMARY.md`
</output>
