---
phase: 04-error-handling
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/api/search/route.ts
  - src/app/api/bank-rates/route.ts
  - src/app/api/apt/route.ts
  - src/app/api/apt/extremes/route.ts
  - src/app/api/analytics/popular/route.ts
  - src/app/api/analytics/pageview/route.ts
  - src/app/api/daily/route.ts
  - src/app/api/dam/content/route.ts
  - src/app/api/push/subscribe/route.ts
  - src/app/api/rate/history/route.ts
  - src/app/api/seeding/route.ts
  - src/app/api/admin/users/route.ts
  - src/app/api/cron/analytics/route.ts
  - src/app/api/cron/coupang/route.ts
  - src/app/api/cron/enrich-complexes/route.ts
  - src/app/api/cron/fetch-bank-rates/route.ts
  - src/app/api/cron/fetch-rates/route.ts
  - src/app/api/cron/fetch-reb-index/route.ts
  - src/app/api/cron/fetch-rents/route.ts
  - src/app/api/cron/generate-cardnews/route.ts
  - src/app/api/cron/generate-report/route.ts
  - src/app/api/cron/generate-seeding/route.ts
  - src/app/api/cron/geocode-complexes/route.ts
  - src/app/api/cron/news/route.ts
  - src/app/api/cron/post-instagram/route.ts
  - src/app/api/cron/refresh-cache/route.ts
  - src/app/api/cron/send-push/route.ts
autonomous: true
requirements:
  - ERR-02
  - ERR-04
must_haves:
  truths:
    - "클라이언트 대면 API 라우트에서 e.message/error.message가 응답에 포함되지 않는다"
    - "모든 API 라우트에서 console.log/warn/error 대신 logger를 사용한다"
    - "모든 크론잡이 에러 발생 시 sendSlackAlert를 호출한다"
  artifacts:
    - path: "src/app/api/search/route.ts"
      provides: "에러 메시지 미노출 패턴 예시"
      contains: "서버 오류가 발생했습니다"
  key_links:
    - from: "src/app/api/**/route.ts"
      to: "src/lib/logger.ts"
      via: "import { logger } from '@/lib/logger'"
      pattern: "logger\\.(info|warn|error)"
    - from: "src/app/api/cron/**/route.ts"
      to: "src/lib/alert.ts"
      via: "import { sendSlackAlert }"
      pattern: "sendSlackAlert"
---

<objective>
모든 API 라우트에서 내부 에러 메시지 노출을 제거하고, 구조화 로깅으로 교체하며, 크론잡에 일관된 Slack 알림을 추가한다.

Purpose: 보안 강화(내부 정보 유출 방지) + 운영 가시성(구조화 로깅 + Slack 알림)
Output: 12개 클라이언트 API 라우트 에러 응답 정리, 15개 크론잡 Slack 알림 일관성 확보
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-error-handling/04-01-SUMMARY.md
@src/lib/logger.ts
@src/lib/alert.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 클라이언트 대면 API 라우트 에러 응답 정리 + logger 교체</name>
  <files>
    src/app/api/search/route.ts,
    src/app/api/bank-rates/route.ts,
    src/app/api/apt/route.ts,
    src/app/api/apt/extremes/route.ts,
    src/app/api/analytics/popular/route.ts,
    src/app/api/analytics/pageview/route.ts,
    src/app/api/daily/route.ts,
    src/app/api/dam/content/route.ts,
    src/app/api/push/subscribe/route.ts,
    src/app/api/rate/history/route.ts,
    src/app/api/seeding/route.ts,
    src/app/api/admin/users/route.ts
  </files>
  <read_first>
    src/lib/logger.ts,
    src/app/api/search/route.ts,
    src/app/api/bank-rates/route.ts,
    src/app/api/apt/route.ts,
    src/app/api/apt/extremes/route.ts,
    src/app/api/push/subscribe/route.ts,
    src/app/api/admin/users/route.ts,
    src/app/api/dam/content/route.ts
  </read_first>
  <action>
12개 클라이언트 대면 API 라우트에 대해 다음 패턴 적용:

1. `import { logger } from "@/lib/logger";` 추가
2. catch 블록에서:
   - `console.error(...)` → `logger.error("설명", { error: e, route: "/api/xxx" })` 로 교체
   - `console.log(...)` → `logger.info(...)` 로 교체
   - 응답에서 `error.message`, `e.message`, `String(e)` 제거
   - 일반 에러 메시지로 교체: `{ error: "서버 오류가 발생했습니다" }`

파일별 구체적 변경:

**src/app/api/search/route.ts (line 163-165):**
- `catch (e: any)` → `catch (e)`
- `console.error("[Search API] Query failed:", e.message ?? e)` → `logger.error("Search query failed", { error: e, route: "/api/search" })`
- `{ results: [], error: e.message }` → `{ results: [], error: "검색 중 오류가 발생했습니다" }`

**src/app/api/bank-rates/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/apt/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/apt/extremes/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/analytics/popular/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/analytics/pageview/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/daily/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/dam/content/route.ts:**
- `{ items: [], error: error.message }` → `{ items: [], error: "서버 오류가 발생했습니다" }` + logger.error 추가
- `{ items: [], error: String(e) }` → 동일 처리
- `{ error: error.message }` (PATCH) → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가
- `{ error: String(e) }` (PATCH) → 동일 처리

**src/app/api/push/subscribe/route.ts:**
- `{ error: error.message }` 및 `{ error: msg }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/rate/history/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/seeding/route.ts:**
- `{ error: error.message }` → `{ error: "서버 오류가 발생했습니다" }` + logger.error 추가

**src/app/api/admin/users/route.ts:**
- `{ error: "Failed to list users", detail: String(e) }` → `{ error: "Failed to list users" }` (detail 제거) + logger.error 추가

주의: 400 Bad Request 같은 의도적 에러 응답(예: "id and status required")은 변경하지 않음 - 이것은 비즈니스 유효성 검사 메시지임.
주의: 404 응답(예: "단지를 찾을 수 없습니다")도 변경하지 않음 - 사용자용 메시지임.
주의: 401/403 응답("Unauthorized", "Forbidden", "Invalid token")도 변경하지 않음 - 표준 인증 메시지임.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -rn "error\.message\|e\.message\|String(e)" src/app/api/ --include="*.ts" | grep -v "/cron/" | grep -v "instanceof Error" | grep "status: 500" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- grep으로 클라이언트 대면 API 라우트의 500 응답에서 error.message/e.message/String(e) 직접 반환 0건
- 모든 수정 파일에 `import { logger }` 존재
- 400/401/403/404 응답의 명시적 메시지는 유지됨
- pnpm build 성공
  </acceptance_criteria>
  <done>12개 클라이언트 대면 API 라우트에서 내부 에러 메시지가 응답에 노출되지 않고, 구조화 로깅으로 서버에 기록된다</done>
</task>

<task type="auto">
  <name>Task 2: 크론잡 logger 교체 + Slack 알림 일관성 확보</name>
  <files>
    src/app/api/cron/analytics/route.ts,
    src/app/api/cron/coupang/route.ts,
    src/app/api/cron/enrich-complexes/route.ts,
    src/app/api/cron/fetch-bank-rates/route.ts,
    src/app/api/cron/fetch-rates/route.ts,
    src/app/api/cron/fetch-reb-index/route.ts,
    src/app/api/cron/fetch-rents/route.ts,
    src/app/api/cron/fetch-transactions/route.ts,
    src/app/api/cron/generate-cardnews/route.ts,
    src/app/api/cron/generate-report/route.ts,
    src/app/api/cron/generate-seeding/route.ts,
    src/app/api/cron/geocode-complexes/route.ts,
    src/app/api/cron/news/route.ts,
    src/app/api/cron/post-instagram/route.ts,
    src/app/api/cron/refresh-cache/route.ts,
    src/app/api/cron/send-push/route.ts
  </files>
  <read_first>
    src/lib/logger.ts,
    src/lib/alert.ts,
    src/app/api/cron/fetch-transactions/route.ts,
    src/app/api/cron/validate-data/route.ts,
    src/app/api/cron/analytics/route.ts,
    src/app/api/cron/fetch-rates/route.ts,
    src/app/api/cron/news/route.ts
  </read_first>
  <action>
15개 크론잡 라우트(validate-data 제외, 이미 Slack 알림 있음)에 대해:

1. `import { logger } from "@/lib/logger";` 추가
2. `import { sendSlackAlert } from "@/lib/alert";` 추가 (없는 경우)
3. `console.log(...)` → `logger.info(...)` 교체
4. `console.error(...)` → `logger.error(...)` 교체
5. 최종 catch 블록 또는 errors 배열이 비어있지 않을 때 sendSlackAlert 호출 추가

Slack 알림 패턴 (fetch-transactions/validate-data 기존 패턴 참고):
```typescript
if (errors.length > 0) {
  await sendSlackAlert(`[크론잡이름] ${errors.length}건 에러: ${errors.slice(0, 3).join(", ")}`);
}
```

또는 최상위 catch에서:
```typescript
catch (e) {
  const msg = e instanceof Error ? e.message : String(e);
  logger.error("Cron job failed", { error: e, cron: "job-name" });
  await sendSlackAlert(`[크론잡이름] 실패: ${msg}`);
  return NextResponse.json({ success: false, error: msg }, { status: 500 });
}
```

주의: 크론잡 응답에서는 e.message를 포함해도 됨 (CRON_SECRET로 보호된 내부 엔드포인트). 여기서는 logger 교체와 Slack 알림 추가에 집중.

주의: fetch-transactions와 validate-data는 이미 sendSlackAlert를 사용하므로, console.log → logger 교체만 수행. Slack 알림 로직은 건드리지 않음.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && echo "=== Slack alert coverage ===" && for f in src/app/api/cron/*/route.ts; do name=$(basename $(dirname $f)); has=$(grep -l "sendSlackAlert" "$f" 2>/dev/null && echo "YES" || echo "NO"); echo "$name: $has"; done</automated>
  </verify>
  <acceptance_criteria>
- 모든 크론잡 라우트에 sendSlackAlert import 존재
- 모든 크론잡 라우트에서 에러 발생 시 sendSlackAlert 호출
- console.log/warn/error가 logger로 교체됨 (grep "console\." src/app/api/cron/ 결과 0건)
- pnpm build 성공
  </acceptance_criteria>
  <done>17개 크론잡 모두에서 에러 시 Slack 알림이 발송되고, 구조화 로깅이 사용된다</done>
</task>

</tasks>

<verification>
- `grep -rn "error\.message\|e\.message" src/app/api/ --include="*.ts" | grep -v "/cron/" | grep "status: 500"` 결과 0건
- `grep -rn "console\." src/app/api/ --include="*.ts"` 결과 0건 (또는 의도적 console만)
- 모든 크론잡에 sendSlackAlert 존재
- `pnpm build` 성공
</verification>

<success_criteria>
- 클라이언트 대면 API에서 내부 에러 메시지 노출 0건
- 모든 API 라우트에서 logger 사용
- 모든 크론잡에서 에러 시 Slack 알림 발송
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-handling/04-02-SUMMARY.md`
</output>
