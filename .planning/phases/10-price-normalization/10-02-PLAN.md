---
phase: 10-price-normalization
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/charts/PriceHistoryChart.tsx
  - src/components/apt/AptDetailClient.tsx
autonomous: false
requirements: [NORM-01, NORM-03, NORM-04, NORM-05]
must_haves:
  truths:
    - "차트에 개별 거래 점(dot)과 3개월 이동중위가 추이선이 함께 표시된다"
    - "직거래는 회색 반투명 점으로 별도 표시되고 추이선 계산에서 제외된다"
    - "중위가 90% 미만 거래는 차트에서 완전히 제외된다"
    - "사용자가 저층 포함/제외 토글로 저층 거래 표시를 전환할 수 있다"
    - "거래 건수 5건 미만 월은 추이선이 점선으로 표시된다"
  artifacts:
    - path: "src/components/charts/PriceHistoryChart.tsx"
      provides: "정규화된 가격 차트 — 거래점 + 이동중위가 추이선 + 직거래 회색 점"
      contains: "movingMedian"
    - path: "src/components/apt/AptDetailClient.tsx"
      provides: "저층 포함/제외 토글 UI + 정규화 필터 적용"
      contains: "includeLowFloor"
  key_links:
    - from: "src/components/apt/AptDetailClient.tsx"
      to: "src/lib/price-normalization.ts"
      via: "import filterTransactions, computeMovingMedian, groupByMonth"
      pattern: "import.*filterTransactions.*price-normalization"
    - from: "src/components/apt/AptDetailClient.tsx"
      to: "src/components/charts/PriceHistoryChart.tsx"
      via: "props: normalDots, directDealDots, trendLine"
      pattern: "normalDots|directDealDots|trendLine"
    - from: "src/components/charts/PriceHistoryChart.tsx"
      to: "recharts"
      via: "ScatterChart + LineChart composite or ComposedChart"
      pattern: "ComposedChart|Scatter|Line"
---

<objective>
PriceHistoryChart를 완전히 재구성하여 정규화된 가격 추이를 표시하고, AptDetailClient에 저층 토글을 추가한다.

Purpose: 사용자가 노이즈 없는 정확한 가격 추이를 볼 수 있게 한다. 개별 거래점 + 3개월 이동중위가 추이선 병행, 직거래 회색 표시, 이상거래 자동 제거.
Output: 완전히 재구성된 PriceHistoryChart + AptDetailClient 저층 토글
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-price-normalization/10-CONTEXT.md
@.planning/phases/10-price-normalization/10-01-SUMMARY.md
@src/components/charts/PriceHistoryChart.tsx
@src/components/apt/AptDetailClient.tsx
@src/lib/price-normalization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild PriceHistoryChart with dots + trend line + direct deal markers</name>
  <files>src/components/charts/PriceHistoryChart.tsx</files>
  <read_first>
    - src/components/charts/PriceHistoryChart.tsx
    - src/lib/price-normalization.ts
    - .planning/phases/10-price-normalization/10-CONTEXT.md
  </read_first>
  <action>
Completely rewrite `PriceHistoryChart.tsx` to accept new data shape and render dots + trend line.

**New props interface:**
```typescript
interface ChartTransaction {
  trade_date: string;
  trade_price: number;
  size_sqm: number;
  deal_type: string | null;
  floor: number;
  isDirectDeal: boolean;  // pre-computed by parent
}

interface TrendPoint {
  month: string;       // YYYY-MM
  median: number;      // 3-month moving median price
  count: number;       // transaction count in window
  isLowConfidence: boolean;  // < 5 transactions
}

interface PriceHistoryChartProps {
  normalDots: ChartTransaction[];      // normal transactions (green dots)
  directDealDots: ChartTransaction[];  // direct deals (gray transparent dots)
  trendLine: TrendPoint[];             // 3-month moving median line
  sizeUnit?: "sqm" | "pyeong";
}
```

**Chart structure using Recharts `ComposedChart`:**
1. **Area/Line for trend**: Render `trendLine` as a smooth line (`type="monotone"`) with green stroke (#059669) and subtle gradient fill underneath.
   - For `isLowConfidence` segments: use `strokeDasharray="5 5"` (dashed line). This requires splitting the trendLine into segments or using a custom line renderer.
   - Implementation approach: Render the full trend line as solid, then overlay a second `<Line>` dataset that only has values for low-confidence months (same coordinates) with dashed stroke. Or use a customized `<Line dot={false}>` with `strokeDasharray` conditional rendering.

2. **Scatter for normal dots**: Render `normalDots` as small green dots (r=3, fill="#059669", opacity=0.7).
   - X axis: trade_date (formatted as MM-DD or YY.MM)
   - Y axis: trade_price

3. **Scatter for direct deal dots**: Render `directDealDots` as gray dots (r=3, fill="#9CA3AF", opacity=0.4).
   - Same axes as normal dots.

4. **X axis**: Show trade dates, `interval="preserveStartEnd"`, font size 11
5. **Y axis**: Price formatter, `domain={["dataMin - 5%", "dataMax + 5%"]}` for some padding
6. **Tooltip**: Show date, price, floor, deal_type for dots. Show month + median + count for trend line hover.

**Data transformation for ComposedChart:**
Since Recharts ComposedChart needs a unified data array for Line but separate arrays for Scatter:
- Use `<ComposedChart>` with `data={trendLine}` for the Line
- Use `<Scatter data={normalDots}>` and `<Scatter data={directDealDots}>` overlaid
- Both Scatter and Line share the same XAxis (time-based) and YAxis (price)

**Alternative simpler approach (recommended):**
Use a single `<ComposedChart>` with merged data where each data point has optional fields:
- Merge all data into one timeline array sorted by date
- Each entry has: `date`, `normalPrice?`, `directPrice?`, `trendMedian?`
- Render `<Line dataKey="trendMedian">` for the trend
- Render `<Scatter dataKey="normalPrice">` with custom shape for green dots
- Render `<Scatter dataKey="directPrice">` with custom shape for gray dots

Actually, since Recharts Scatter within ComposedChart needs the data on the chart data prop, the cleanest approach is:

**Final approach: Use two chart layers.**
1. Build a unified monthly data array for the trend line
2. Overlay individual transaction dots using Recharts `customized` render or `<Scatter>` with `data` prop
3. Keep the existing responsive container sizing: `h-[280px] sm:h-[240px]`

Remove the old `AreaChart` implementation entirely. Remove the old `Transaction` interface (it's no longer used). Remove old internal size tab logic (size selection is now fully external from AptDetailClient).

Keep the `formatPrice` and `sqmToPyeong` local helpers. Keep `CustomTooltip` but update it to show deal_type and floor info.

**Header section:**
- Keep title "가격 추이"
- Show selected size label (passed via prop or derived)
- Do NOT render size tabs inside the chart (per NORM-01, size selection is in AptDetailClient)
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx tsc --noEmit src/components/charts/PriceHistoryChart.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ComposedChart" src/components/charts/PriceHistoryChart.tsx` matches at least once
    - `grep "trendLine" src/components/charts/PriceHistoryChart.tsx` matches at least twice (prop + usage)
    - `grep "normalDots" src/components/charts/PriceHistoryChart.tsx` matches at least twice
    - `grep "directDealDots" src/components/charts/PriceHistoryChart.tsx` matches at least twice
    - `grep "isLowConfidence" src/components/charts/PriceHistoryChart.tsx` matches (dashed line logic)
    - `grep "#9CA3AF" src/components/charts/PriceHistoryChart.tsx` matches (gray color for direct deals)
    - `grep "strokeDasharray" src/components/charts/PriceHistoryChart.tsx` matches (dashed line for low confidence)
    - `grep "AreaChart" src/components/charts/PriceHistoryChart.tsx` returns 0 hits (old chart removed)
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>PriceHistoryChart renders green dots for normal transactions, gray transparent dots for direct deals, and a 3-month moving median trend line (dashed for low-confidence months). Old AreaChart implementation fully replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire normalization pipeline in AptDetailClient + add low-floor toggle</name>
  <files>src/components/apt/AptDetailClient.tsx</files>
  <read_first>
    - src/components/apt/AptDetailClient.tsx
    - src/lib/price-normalization.ts
    - src/components/charts/PriceHistoryChart.tsx
    - .planning/phases/10-price-normalization/10-CONTEXT.md
  </read_first>
  <action>
Modify `AptDetailClient.tsx` to:

1. **Import normalization functions:**
   ```typescript
   import { filterTransactions, computeMovingMedian, groupByMonth, computeMedianPrice, LOW_FLOOR_MAX } from "@/lib/price-normalization";
   ```

2. **Add low-floor toggle state:**
   ```typescript
   const [includeLowFloor, setIncludeLowFloor] = useState(false); // default: exclude low floors (per CONTEXT)
   ```

3. **Compute recent median for the selected size** (needed for filtering):
   ```typescript
   const recentMedian = useMemo(() => {
     if (!selectedSize) return 0;
     const last6Months = new Date();
     last6Months.setMonth(last6Months.getMonth() - 6);
     const dateStr = last6Months.toISOString().slice(0, 10);
     const recentPrices = saleTxns
       .filter(t => t.size_sqm === selectedSize && t.trade_date >= dateStr && t.floor > LOW_FLOOR_MAX)
       .map(t => t.trade_price);
     return computeMedianPrice(recentPrices);
   }, [saleTxns, selectedSize]);
   ```

4. **Apply filterTransactions pipeline:**
   ```typescript
   const { normal, directDeals } = useMemo(() => {
     const sizeFiltered = selectedSize ? saleTxns.filter(t => t.size_sqm === selectedSize) : saleTxns;
     return filterTransactions(sizeFiltered, {
       excludeLowFloor: !includeLowFloor,
       recentMedian,
     });
   }, [saleTxns, selectedSize, includeLowFloor, recentMedian]);
   ```

5. **Compute trend line data:**
   ```typescript
   const trendLine = useMemo(() => {
     const monthly = groupByMonth(normal);
     return computeMovingMedian(monthly);
   }, [normal]);
   ```

6. **Pass new props to PriceHistoryChart:**
   ```tsx
   <PriceHistoryChart
     normalDots={normal.map(t => ({
       trade_date: t.trade_date,
       trade_price: t.trade_price,
       size_sqm: t.size_sqm,
       deal_type: t.deal_type,
       floor: t.floor,
       isDirectDeal: false,
     }))}
     directDealDots={directDeals.map(t => ({
       trade_date: t.trade_date,
       trade_price: t.trade_price,
       size_sqm: t.size_sqm,
       deal_type: t.deal_type,
       floor: t.floor,
       isDirectDeal: true,
     }))}
     trendLine={trendLine}
     sizeUnit={sizeUnit}
   />
   ```

7. **Add low-floor toggle UI** below the chart:
   ```tsx
   <div className="mt-2 flex items-center justify-between">
     <p className="text-[10px]" style={{ color: "var(--color-text-tertiary)" }}>
       * 추이선: 3개월 이동중위가 · 직거래(회색 점)는 추이선 미반영
     </p>
     <label className="flex items-center gap-1.5 cursor-pointer">
       <span className="text-[10px]" style={{ color: "var(--color-text-tertiary)" }}>저층 포함</span>
       <input
         type="checkbox"
         checked={includeLowFloor}
         onChange={(e) => setIncludeLowFloor(e.target.checked)}
         className="h-3.5 w-3.5 rounded border-gray-300 accent-brand-600"
       />
     </label>
   </div>
   ```

8. **Remove old chart rendering code** that does `.filter((t) => t.floor > LOW_FLOOR_MAX).map(...)` pattern. The filtering is now done via `filterTransactions`.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx tsc --noEmit src/components/apt/AptDetailClient.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "filterTransactions" src/components/apt/AptDetailClient.tsx` matches at least once
    - `grep "computeMovingMedian" src/components/apt/AptDetailClient.tsx` matches at least once
    - `grep "groupByMonth" src/components/apt/AptDetailClient.tsx` matches at least once
    - `grep "includeLowFloor" src/components/apt/AptDetailClient.tsx` matches at least 3 times (state + toggle + usage)
    - `grep "normalDots" src/components/apt/AptDetailClient.tsx` matches (prop passed to chart)
    - `grep "directDealDots" src/components/apt/AptDetailClient.tsx` matches (prop passed to chart)
    - `grep "trendLine" src/components/apt/AptDetailClient.tsx` matches (prop passed to chart)
    - `grep "recentMedian" src/components/apt/AptDetailClient.tsx` matches (median computation)
    - `grep "저층 포함" src/components/apt/AptDetailClient.tsx` matches (toggle label)
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>AptDetailClient applies full normalization pipeline: size filtering -> low-floor toggle -> anomaly exclusion -> trend line computation. Chart receives normalized data. Toggle allows user to include/exclude low-floor transactions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify normalized chart visually</name>
  <files>src/components/charts/PriceHistoryChart.tsx, src/components/apt/AptDetailClient.tsx</files>
  <action>
Visual verification of all normalization features applied to the chart. No code changes — human reviews the running application.
  </action>
  <what-built>
    Price normalization engine applied to the chart:
    1. "전체" 면적 탭 제거, 최다 거래 면적 자동 선택
    2. 개별 거래는 초록색 점, 직거래는 회색 반투명 점
    3. 3개월 이동중위가 추이선 (거래 5건 미만 월은 점선)
    4. 이상거래(직거래+중위가 90% 미만) 자동 제거
    5. "저층 포함" 토글로 저층(1~3층) 거래 표시/숨김 전환
  </what-built>
  <how-to-verify>
    1. `npx next dev`로 개발 서버 실행
    2. 거래가 많은 아파트 상세 페이지 접속 (예: 서울 아파트)
    3. 확인사항:
       a. 면적 선택 영역에 "전체" 버튼 없음
       b. 페이지 로드 시 가장 거래 많은 면적이 자동 선택됨
       c. 차트에 초록색 점(개별 거래) + 부드러운 추이선이 함께 보임
       d. 직거래 거래가 있으면 회색 반투명 점으로 표시
       e. "저층 포함" 체크박스가 차트 아래에 존재
       f. 체크박스 토글 시 차트가 업데이트됨
       g. 추이선 annotation "3개월 이동중위가" 표시
  </how-to-verify>
  <verify>Human visual inspection of running dev server</verify>
  <done>User confirms all 7 verification items pass</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — full project type check passes
- `npx next build` — production build succeeds
- Chart visually shows dots + trend line + direct deal markers
- Low-floor toggle works
- "전체" tab removed, auto-selects most traded size
</verification>

<success_criteria>
1. 차트에 개별 거래 점과 3개월 이동중위가 추이선이 병행 표시된다
2. 직거래는 회색 반투명 점으로 별도 표시, 추이선 계산에서 제외
3. 이상거래(중위가 90% 미만)는 차트에서 완전히 숨겨진다
4. "저층 포함/제외" 토글이 작동한다
5. 거래 건수 5건 미만 월은 추이선 점선 표시
6. TypeScript 에러 없음, 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/10-price-normalization/10-02-SUMMARY.md`
</output>
