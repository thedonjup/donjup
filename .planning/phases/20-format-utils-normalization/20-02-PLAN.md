---
phase: 20-format-utils-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/components/charts/PriceHistoryChart.tsx
  - src/app/apt/[region]/[slug]/opengraph-image.tsx
  - src/lib/cardnews/templates/rank-item.tsx
  - src/app/api/cron/generate-seeding/route.ts
  - src/components/apt/AptDetailClient.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "PriceHistoryChart imports formatPrice and sqmToPyeong from @/lib/format, not local definitions"
    - "opengraph-image imports formatPrice from @/lib/format, not local definition"
    - "rank-item imports formatPrice from @/lib/format, not local definition"
    - "generate-seeding uses formatKrw (not formatPrice) from @/lib/format for 원-unit values"
    - "AptDetailClient imports formatPriceShort from @/lib/format, not local inner function"
  artifacts:
    - path: "src/components/charts/PriceHistoryChart.tsx"
      provides: "Chart with central format imports"
      contains: "import.*from.*@/lib/format"
    - path: "src/app/api/cron/generate-seeding/route.ts"
      provides: "Seeding cron with formatKrw"
      contains: "formatKrw"
  key_links:
    - from: "src/components/charts/PriceHistoryChart.tsx"
      to: "src/lib/format.ts"
      via: "import { formatPrice, formatPriceAxis, sqmToPyeong }"
      pattern: "import.*formatPrice.*from.*@/lib/format"
    - from: "src/app/api/cron/generate-seeding/route.ts"
      to: "src/lib/format.ts"
      via: "import { formatKrw }"
      pattern: "import.*formatKrw.*from.*@/lib/format"
---

<objective>
Replace all local formatPrice/formatPriceShort/formatPriceAxis/formatKrw duplicates with imports from the central module.

Purpose: Eliminate price formatting duplicates (DATA-01, DATA-04). Critical: generate-seeding uses 원 units, must use formatKrw not formatPrice.
Output: 5 files updated, zero local price format definitions remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-format-utils-normalization/20-RESEARCH.md
@.planning/phases/20-format-utils-normalization/20-01-SUMMARY.md

@src/lib/format.ts
</context>

<interfaces>
<!-- Central format module exports (from Plan 01) -->
From src/lib/format.ts:
```typescript
export function formatPrice(priceInManWon: number): string   // 만원 input → "3억 2,000만"
export function formatKrw(won: number): string               // 원 input → "3억 2,000만원"
export function sqmToPyeong(sqm: number): number             // returns float with 1 decimal
export function formatSizeWithPyeong(sqm: number): string    // "84.93㎡ (25.7평)"
export function formatPriceShort(v: number): string           // "3.2억", "8,500만"
export function formatPriceAxis(v: number): string            // "3.2억" (chart Y axis)
export function formatNullable(v, fallback?): string          // null/0/"" → "-"
export const formatArea: (sqm: number) => string             // alias of formatSizeWithPyeong
export function formatDateKo(dateStr: string): string         // "YYYY-MM-DD"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace price format duplicates in chart + OG image + cardnews + seeding</name>
  <files>src/components/charts/PriceHistoryChart.tsx, src/app/apt/[region]/[slug]/opengraph-image.tsx, src/lib/cardnews/templates/rank-item.tsx, src/app/api/cron/generate-seeding/route.ts</files>
  <read_first>
    - src/lib/format.ts
    - src/components/charts/PriceHistoryChart.tsx
    - src/app/apt/[region]/[slug]/opengraph-image.tsx
    - src/lib/cardnews/templates/rank-item.tsx
    - src/app/api/cron/generate-seeding/route.ts
  </read_first>
  <action>
    **PriceHistoryChart.tsx:**
    1. Remove the local `function formatPrice(` definition (lines ~50-57)
    2. Remove the local `const sqmToPyeong =` definition (line ~59)
    3. Remove the local `formatPriceAxis`-equivalent function (lines ~63-68) if it exists
    4. Add import: `import { formatPrice, formatPriceAxis, sqmToPyeong } from '@/lib/format';`
    5. Verify all call sites still reference the same function names

    **opengraph-image.tsx:**
    1. Remove the local `function formatPrice(` definition (lines ~11-18)
    2. Add import: `import { formatPrice } from '@/lib/format';`

    **rank-item.tsx:**
    1. Remove the local `function formatPrice(` definition (lines ~9-16)
    2. Add import: `import { formatPrice } from '@/lib/format';`

    **generate-seeding/route.ts:**
    1. Remove the local `function formatPrice(` definition (lines ~39-45)
    2. Add import: `import { formatKrw } from '@/lib/format';`
    3. CRITICAL: Replace all call sites of local `formatPrice(wonValue)` with `formatKrw(wonValue)` — the local function takes 원 units, NOT 만원. The central `formatKrw` handles 원 → "N억 N만원" conversion correctly.
    4. Double-check: if the local function had any different formatting (e.g., no "원" suffix), verify that `formatKrw` output is acceptable in the seeding context.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx tsc --noEmit 2>&1 | head -20 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function formatPrice" src/components/charts/PriceHistoryChart.tsx` returns no matches
    - `grep -n "function formatPrice" src/app/apt/[region]/[slug]/opengraph-image.tsx` returns no matches
    - `grep -n "function formatPrice" src/lib/cardnews/templates/rank-item.tsx` returns no matches
    - `grep -n "function formatPrice" src/app/api/cron/generate-seeding/route.ts` returns no matches
    - `grep -n "formatKrw" src/app/api/cron/generate-seeding/route.ts` returns at least 1 match
    - `grep -n "from '@/lib/format'" src/components/charts/PriceHistoryChart.tsx` returns at least 1 match
    - TypeScript compilation succeeds (`npx tsc --noEmit` exits 0)
    - `npm test` passes
  </acceptance_criteria>
  <done>All 4 files use central formatPrice/formatKrw imports. No local price format functions remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace formatPriceShort in AptDetailClient</name>
  <files>src/components/apt/AptDetailClient.tsx</files>
  <read_first>
    - src/components/apt/AptDetailClient.tsx
    - src/lib/format.ts
  </read_first>
  <action>
    1. Find the local `formatPriceShort` inner function (lines ~189-196 per research, may be inside component body)
    2. Remove the local function definition entirely
    3. Add `formatPriceShort` to the existing import from `@/lib/format` (AptDetailClient likely already imports `sqmToPyeong` — extend that import)
    4. Also check: if AptDetailClient has a local `sqmToPyeong` (line ~21 per research), remove it and ensure it's imported from `@/lib/format`
    5. Verify all call sites of `formatPriceShort(...)` and `sqmToPyeong(...)` still work with the central versions

    The central `formatPriceShort` in format.ts matches the local logic: `>= 10000` → "N.M억", else "N,NNN만".
    The central `sqmToPyeong` returns a number with 1 decimal — check if AptDetailClient expects string (likely not, it's a client component doing math).
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx tsc --noEmit 2>&1 | head -20 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function formatPriceShort\|const formatPriceShort\|=> formatPriceShort" src/components/apt/AptDetailClient.tsx` returns no matches for local definitions
    - `grep -n "const sqmToPyeong\|function sqmToPyeong" src/components/apt/AptDetailClient.tsx` returns no matches
    - `grep -n "from '@/lib/format'" src/components/apt/AptDetailClient.tsx` returns a match containing `formatPriceShort`
    - TypeScript compilation succeeds
    - `npm test` passes
  </acceptance_criteria>
  <done>AptDetailClient uses central formatPriceShort and sqmToPyeong. No local price/area format functions in this file.</done>
</task>

</tasks>

<verification>
- `grep -rn "function formatPrice" src/ --include="*.ts" --include="*.tsx"` returns ONLY `src/lib/format.ts` matches
- `grep -rn "function formatPriceShort\|const formatPriceShort" src/ --include="*.ts" --include="*.tsx"` returns ONLY `src/lib/format.ts`
- TypeScript compiles cleanly
- All existing tests pass
</verification>

<success_criteria>
- Zero local formatPrice definitions outside src/lib/format.ts
- Zero local formatPriceShort definitions outside src/lib/format.ts
- generate-seeding uses formatKrw (원 unit), not formatPrice (만원 unit)
- Build succeeds, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-format-utils-normalization/20-02-SUMMARY.md`
</output>
