---
phase: 09-mobile-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/new-highs/page.tsx
  - src/app/rate/page.tsx
  - src/components/home/HeroSection.tsx
autonomous: true
requirements:
  - MOBILE-06
  - MOBILE-07
  - MOBILE-09

must_haves:
  truths:
    - "신고가 페이지가 모바일에서 카드 형태로 표시된다"
    - "금리 페이지 은행명이 BANK_코드가 아닌 한국어로 표시된다"
    - "홈 히어로 텍스트가 모바일에서 자연스럽게 줄바꿈된다"
  artifacts:
    - path: "src/app/new-highs/page.tsx"
      provides: "모바일 카드 레이아웃"
      contains: "sm:hidden"
    - path: "src/app/rate/page.tsx"
      provides: "BANK_UNKNOWN 등 fallback 한국어화"
      contains: "BANK_LABELS"
    - path: "src/components/home/HeroSection.tsx"
      provides: "모바일 타이포그래피 개선"
      contains: "sm:text-4xl"
  key_links:
    - from: "src/app/new-highs/page.tsx"
      to: "mobile card layout"
      via: "sm:hidden responsive class"
      pattern: "sm:hidden"
---

<objective>
신고가 모바일 카드 + 금리 은행명 한국어화 + 홈 히어로 모바일 타이포그래피

Purpose: 3개 독립 페이지의 모바일 가독성/UX 개선
Output: 각 페이지에서 모바일 최적화된 UI 렌더링
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mobile-ui/09-CONTEXT.md
@src/app/new-highs/page.tsx
@src/app/rate/page.tsx
@src/components/home/HeroSection.tsx
@src/app/today/page.tsx (카드 레이아웃 참고 — 이미 모바일 카드 구현됨)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 신고가 페이지 모바일 카드 레이아웃 추가 (MOBILE-09)</name>
  <files>src/app/new-highs/page.tsx</files>
  <read_first>src/app/today/page.tsx (lines 116-178 — 기존 모바일 카드 패턴 참조)</read_first>
  <action>
today/page.tsx의 모바일 카드 패턴을 참고하여 new-highs/page.tsx에 동일 구조의 모바일 카드 추가.

현재 new-highs는 테이블만 있음 (line 105-162). 테이블 div에 `hidden sm:block` 추가하고, 그 위에 모바일 카드 블록 추가:

1. 기존 테이블 div(`overflow-x-auto rounded-2xl...`)에 `hidden sm:block` 클래스 추가
2. 테이블 위에 `<div className="space-y-2 sm:hidden">` 블록 추가
3. 카드 내부 구조:
   - 첫 줄: 왼쪽에 순위 badge(`rank-badge rank-badge-rise text-[11px]`) + 단지명(`truncate text-sm font-semibold t-text`), 오른쪽에 거래가(`text-sm font-bold tabular-nums t-rise`)
   - 둘째 줄: 지역명 + 면적(평) + 거래일 (`text-xs t-text-tertiary`)
   - 거래유형 badge (직거래인 경우만 표시)
4. 카드는 `<Link>` 래핑, `card-hover block rounded-xl border t-border t-card px-4 py-3.5` 스타일
5. `style={{ WebkitTapHighlightColor: "transparent", minHeight: 64 }}` 추가
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "sm:hidden" src/app/new-highs/page.tsx 결과 >= 1
    - grep -c "hidden sm:block" src/app/new-highs/page.tsx 결과 >= 1
    - grep "rank-badge" src/app/new-highs/page.tsx 결과 존재
  </acceptance_criteria>
  <done>모바일에서 신고가 페이지가 카드 형태로 표시되고, 데스크탑은 기존 테이블 유지</done>
</task>

<task type="auto">
  <name>Task 2: 금리 은행명 fallback 강화 + 홈 히어로 모바일 타이포그래피 (MOBILE-06, MOBILE-07)</name>
  <files>src/app/rate/page.tsx, src/components/home/HeroSection.tsx</files>
  <read_first>src/app/rate/page.tsx (lines 19-38, 187), src/components/home/HeroSection.tsx (lines 29-80)</read_first>
  <action>
**rate/page.tsx (MOBILE-07):**
BANK_LABELS 맵에 누락 가능한 은행 코드 추가. 현재 18개 은행이 정의되어 있으나, 새로운 은행 코드가 추가될 수 있으므로:
- BANK_LABELS에 `BANK_UNKNOWN: "기타"` 추가
- fallback 표현 개선: line 187에서 `bank.rate_type.replace("BANK_", "")` → `BANK_LABELS[bank.rate_type] ?? bank.rate_type.replace(/^BANK_/, "").replace(/_/g, " ")` (이미 이렇게 되어 있으나, 은행별 금리 섹션의 모바일 테이블도 가로 스크롤 없이 읽히도록 모바일 카드로 전환)
- 은행별 주담대 금리 테이블(line 173-214): 모바일에서 카드로 전환. 테이블에 `hidden sm:block` 추가, 위에 `sm:hidden` 카드 리스트 추가
- 카드: 은행명 + 최저금리 + 변동bp + 기준일을 2줄로 배치

**HeroSection.tsx (MOBILE-06):**
모바일에서 히어로 텍스트가 자연스럽게 줄바꿈되도록:
1. h1 태그의 `text-4xl` → `text-2xl sm:text-4xl` (모바일에서 작게)
2. `sm:text-5xl lg:text-6xl` 유지
3. 컨테이너 padding: `py-14 sm:py-20` → `py-10 sm:py-20` (모바일 여백 줄임)
4. 서브텍스트 `text-lg` → `text-base sm:text-lg`
5. 퍼센트 badge의 `px-3 py-1` → `px-2 py-0.5 sm:px-3 sm:py-1` (모바일에서 작게)
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-2xl sm:text-4xl" src/components/home/HeroSection.tsx 결과 존재
    - grep "BANK_UNKNOWN" src/app/rate/page.tsx 결과 존재
    - grep "sm:hidden" src/app/rate/page.tsx 결과 >= 1 (은행 카드 섹션)
  </acceptance_criteria>
  <done>히어로 텍스트가 모바일에서 2xl 크기로 자연스럽게 줄바꿈되고, 금리 은행명이 항상 한국어로 표시되며 모바일에서 카드로 전환됨</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- 각 파일에서 모바일/데스크탑 분기 클래스 존재 확인
</verification>

<success_criteria>
- 신고가 페이지: 모바일 카드 + 데스크탑 테이블 분리 렌더링
- 금리 페이지: 은행명 한국어 + 모바일 카드
- 히어로: 모바일 타이포그래피 최적화
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-ui/09-01-SUMMARY.md`
</output>
