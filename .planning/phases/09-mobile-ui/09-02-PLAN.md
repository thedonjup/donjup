---
phase: 09-mobile-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/MobileNav.tsx
  - src/components/apt/AptDetailClient.tsx
  - src/components/charts/PriceHistoryChart.tsx
autonomous: true
requirements:
  - MOBILE-02
  - MOBILE-03
  - MOBILE-04

must_haves:
  truths:
    - "사이드 메뉴 열었을 때 뒤 컨텐츠 터치가 완전히 차단된다"
    - "면적 선택 UI가 수평 스크롤 1줄 칩으로 표시된다"
    - "차트가 모바일에서 280px 높이로 표시되고 레이블이 겹치지 않는다"
  artifacts:
    - path: "src/components/layout/MobileNav.tsx"
      provides: "backdrop 터치 차단"
      contains: "pointer-events"
    - path: "src/components/apt/AptDetailClient.tsx"
      provides: "수평 스크롤 칩 UI"
      contains: "overflow-x-auto"
    - path: "src/components/charts/PriceHistoryChart.tsx"
      provides: "모바일 차트 높이 280px"
      contains: "280"
  key_links:
    - from: "src/components/layout/MobileNav.tsx"
      to: "backdrop overlay"
      via: "z-index + pointer-events"
      pattern: "zIndex.*999"
    - from: "src/components/apt/AptDetailClient.tsx"
      to: "size chips"
      via: "overflow-x-auto whitespace-nowrap"
      pattern: "overflow-x-auto"
---

<objective>
MobileNav 터치 차단 수정 + 면적 선택 수평 칩 + 차트 모바일 최적화

Purpose: 모바일 인터랙션 핵심 3건 수정 — 메뉴, 면적 선택, 차트
Output: 3개 컴포넌트의 모바일 UX 개선
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mobile-ui/09-CONTEXT.md
@src/components/layout/MobileNav.tsx
@src/components/apt/AptDetailClient.tsx
@src/components/charts/PriceHistoryChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: MobileNav backdrop 터치 차단 수정 (MOBILE-02)</name>
  <files>src/components/layout/MobileNav.tsx</files>
  <read_first>src/components/layout/MobileNav.tsx (lines 100-117 — backdrop 구현부)</read_first>
  <action>
현재 backdrop이 `zIndex: 9990`으로 설정되어 있고 Drawer가 `zIndex: 9991`. 문제는:
1. backdrop 클릭은 되지만, 터치 이벤트가 관통할 수 있음
2. body overflow hidden이 설정되어 있지만 backdrop이 터치를 완전 차단하지 않을 수 있음

수정사항:
1. Backdrop div에 `touchAction: "none"` 추가 (터치 관통 방지)
2. Backdrop div에 `WebkitTapHighlightColor: "transparent"` 추가
3. Backdrop의 `onClick` 외에 `onTouchStart={(e) => e.preventDefault()}` 추가하되, onClick도 유지 (클릭으로 닫기)
4. 실제로는 backdrop onClick으로 충분하지만, 혹시 모를 관통을 위해 backdrop에 `pointerEvents: "auto"` 명시 추가

핵심 수정 — Backdrop div style에 추가:
```
touchAction: "none",
WebkitTapHighlightColor: "transparent",
```
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && grep -c "touchAction" src/components/layout/MobileNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "touchAction" src/components/layout/MobileNav.tsx 결과 >= 1
    - grep "zIndex.*999" src/components/layout/MobileNav.tsx 결과 >= 2 (backdrop + drawer)
  </acceptance_criteria>
  <done>사이드 메뉴 열렸을 때 backdrop이 터치를 완전 차단하고, backdrop 클릭으로 메뉴가 닫힘</done>
</task>

<task type="auto">
  <name>Task 2: 면적 선택 수평 스크롤 칩 + 차트 모바일 최적화 (MOBILE-03, MOBILE-04)</name>
  <files>src/components/apt/AptDetailClient.tsx, src/components/charts/PriceHistoryChart.tsx</files>
  <read_first>src/components/apt/AptDetailClient.tsx (lines 129-186 — 면적 선택 UI), src/components/charts/PriceHistoryChart.tsx (line 107 — ResponsiveContainer height)</read_first>
  <action>
**AptDetailClient.tsx (MOBILE-03):**
면적 선택 영역(line 146: `flex flex-wrap gap-2`)을 모바일에서 수평 스크롤 1줄로 변경:

1. `<div className="flex flex-wrap gap-2">` → `<div className="flex gap-2 overflow-x-auto whitespace-nowrap pb-2 scrollbar-hide sm:flex-wrap sm:overflow-visible sm:whitespace-normal sm:pb-0">`
2. 각 면적 버튼에 `shrink-0` 추가: `className="shrink-0 rounded-xl px-3 py-2 ..."` (모바일에서 줄어들지 않도록)
3. "전체" 버튼에도 동일하게 `shrink-0` 추가
4. 면적 버튼 내부의 가격 상세 정보(고층/저층/전세)는 모바일에서 숨김: `<div className="hidden sm:block text-[10px] mt-0.5 tabular-nums" ...>` (3줄 정보가 칩에 과다)
5. globals.css에 `.scrollbar-hide` 유틸리티가 없다면 inline style `scrollbarWidth: "none"` + `msOverflowStyle: "none"` + `WebkitOverflowScrolling: "touch"` 사용

**PriceHistoryChart.tsx (MOBILE-04):**
1. ResponsiveContainer height를 반응형으로: `height={240}` → 조건부 렌더링 대신 CSS 활용. 방법: ResponsiveContainer를 감싸는 div에 `className="h-[280px] sm:h-[240px]"` 추가하고, ResponsiveContainer에 `height="100%"` 적용
2. Y축 레이블 간소화: `width={70}` → `width={60}`, tickFormatter에서 모바일 감지 없이 간소화된 포맷 사용. 현재 `formatPrice(v)` 사용 중인데, 이미 "1억", "2억 5000만" 형태. 모바일에서 "1억", "2.5억" 형태로 줄이기: Y축 tickFormatter를 `(v) => v >= 10000 ? \`${(v/10000).toFixed(v % 10000 === 0 ? 0 : 1)}억\` : \`${v.toLocaleString()}만\`` 으로 변경
3. X축 interval을 `"preserveStartEnd"` 유지 (이미 적용됨)
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "overflow-x-auto" src/components/apt/AptDetailClient.tsx 결과 >= 1
    - grep "shrink-0" src/components/apt/AptDetailClient.tsx 결과 >= 2
    - grep "280" src/components/charts/PriceHistoryChart.tsx 결과 >= 1
    - grep 'height="100%"' src/components/charts/PriceHistoryChart.tsx 결과 >= 1
  </acceptance_criteria>
  <done>면적 선택이 모바일에서 수평 스크롤 1줄 칩이고, 차트가 모바일 280px/데스크탑 240px로 렌더링되며 Y축 레이블이 간소화됨</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- MobileNav backdrop에 touchAction 존재
- AptDetailClient에 overflow-x-auto + shrink-0 존재
- PriceHistoryChart에 280px 높이 설정 존재
</verification>

<success_criteria>
- 사이드 메뉴 터치 관통 완전 차단
- 면적 15종 이상일 때도 1줄 수평 스크롤
- 차트 모바일에서 280px + 간소화 레이블
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-ui/09-02-SUMMARY.md`
</output>
