---
phase: 11-jeonse-gap-analysis
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/charts/JeonseRatioChart.tsx
  - src/components/apt/AptDetailClient.tsx
autonomous: true
requirements: [GAP-03]
must_haves:
  truths:
    - "면적별 전세가율 추이 차트가 PriceHistoryChart 아래에 표시된다"
    - "X축은 월별, Y축은 전세가율(%)이다"
    - "데이터 5건 미만 월은 점선으로 표시된다"
    - "전세 데이터 없는 면적은 추이 차트가 미표시된다"
  artifacts:
    - path: "src/components/charts/JeonseRatioChart.tsx"
      provides: "전세가율 추이 소형 LineChart"
      exports: ["default"]
      min_lines: 60
    - path: "src/components/apt/AptDetailClient.tsx"
      provides: "JeonseRatioChart 렌더링 연결"
      contains: "JeonseRatioChart"
  key_links:
    - from: "AptDetailClient.tsx"
      to: "JeonseRatioChart.tsx"
      via: "import + selectedSize filtered saleTxns/rentTxns props"
      pattern: "import.*JeonseRatioChart"
    - from: "JeonseRatioChart.tsx"
      to: "price-normalization.ts"
      via: "groupByMonth + computeMedianPrice imports"
      pattern: "import.*groupByMonth.*computeMedianPrice"
---

<objective>
면적별 전세가율 추이 차트를 아파트 상세 페이지에 추가한다.

Purpose: 투자자가 전세가율의 시간에 따른 변화를 추적하여 갭 리스크 트렌드를 파악할 수 있도록 한다.
Output: JeonseRatioChart 신규 컴포넌트 + AptDetailClient에 배치.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-jeonse-gap-analysis/11-CONTEXT.md
@.planning/phases/11-jeonse-gap-analysis/11-RESEARCH.md
@.planning/phases/11-jeonse-gap-analysis/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: extended sizePriceMap in AptDetailClient -->
<!-- Plan 02 does NOT depend on sizePriceMap — it receives raw txns and computes monthly ratios independently -->

From src/components/apt/AptDetailClient.tsx:
```typescript
export interface AptTransaction {
  id: string; trade_date: string; trade_price: number; size_sqm: number;
  floor: number; deal_type: string | null;
  // ...other fields
}

export interface AptRentTransaction {
  id: string; size_sqm: number; floor: number | null;
  deposit: number; monthly_rent: number; rent_type: string;
  contract_type: string | null; trade_date: string;
}
```

From src/lib/price-normalization.ts:
```typescript
export function groupByMonth(txns: { trade_date: string; trade_price: number }[]): { month: string; prices: number[] }[];
export function computeMedianPrice(prices: number[]): number;
```

PriceHistoryChart dual-line pattern (solid + dashed for low confidence):
```tsx
// Two Line components: one for solidRatio (high confidence), one for dashedRatio (low confidence)
// Data points: { month, solidRatio: isLowConfidence ? null : ratio, dashedRatio: isLowConfidence ? ratio : null }
<Line dataKey="solidRatio" stroke="#3B82F6" strokeWidth={2} connectNulls={false} dot={false} />
<Line dataKey="dashedRatio" stroke="#3B82F6" strokeWidth={2} strokeDasharray="5 5" connectNulls={false} dot={false} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JeonseRatioChart component</name>
  <files>src/components/charts/JeonseRatioChart.tsx</files>
  <read_first>
    - src/components/charts/PriceHistoryChart.tsx (dual-line solid/dashed pattern, Recharts imports, Tooltip pattern)
    - src/lib/price-normalization.ts (groupByMonth signature at line 175, computeMedianPrice at line 53)
    - .planning/phases/11-jeonse-gap-analysis/11-CONTEXT.md (D-10 through D-13)
  </read_first>
  <action>
Create new file `src/components/charts/JeonseRatioChart.tsx`:

1. **Props interface:**
   ```typescript
   interface JeonseRatioChartProps {
     saleTxns: AptTransaction[];
     rentTxns: AptRentTransaction[];
   }
   ```
   Import `AptTransaction` and `AptRentTransaction` from `@/components/apt/AptDetailClient`.

2. **Monthly ratio computation** (D-12): Inside a `useMemo`, compute monthly jeonse ratio trend:
   - Filter `rentTxns` to pure jeonse only: `rent_type === "전세" && monthly_rent === 0` (D-01 compliance)
   - Map filtered rent txns to `{ trade_date: r.trade_date, trade_price: r.deposit }` for compatibility with `groupByMonth`
   - Call `groupByMonth()` on mapped rent data -> `rentByMonth`
   - Call `groupByMonth()` on `saleTxns` -> `saleByMonth`
   - Create Maps from both arrays keyed by `month`
   - Find common months (both rent and sale data exist)
   - For each common month: `ratio = computeMedianPrice(rentPrices) / computeMedianPrice(salePrices) * 100`, rounded to 1 decimal
   - Mark `isLowConfidence = (rentCount + saleCount) < 5` (D-13)
   - Return sorted array of `{ month, ratio, isLowConfidence, rentCount, saleCount }`

3. **Chart data transformation** for dual-line pattern:
   ```typescript
   const chartData = ratioPoints.map((p) => ({
     month: p.month,
     solidRatio: p.isLowConfidence ? null : p.ratio,
     dashedRatio: p.isLowConfidence ? p.ratio : null,
   }));
   ```

4. **Render** (D-10: height 160px, separate LineChart below PriceHistoryChart):
   ```tsx
   "use client";
   // Import: LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer from "recharts"
   // Import: groupByMonth, computeMedianPrice from "@/lib/price-normalization"
   // Import: AptTransaction, AptRentTransaction from "@/components/apt/AptDetailClient"

   export default function JeonseRatioChart({ saleTxns, rentTxns }: JeonseRatioChartProps) {
     // ... useMemo for ratioPoints and chartData ...

     if (chartData.length < 2) return null;  // D-14: not enough data

     return (
       <div>
         <h3 className="mb-2 text-sm font-bold t-text">전세가율 추이</h3>
         <ResponsiveContainer width="100%" height={160}>
           <LineChart data={chartData} margin={{ top: 5, right: 5, bottom: 5, left: 5 }}>
             <XAxis
               dataKey="month"
               tick={{ fontSize: 10, fill: "var(--color-text-tertiary)" }}
               tickLine={false}
               axisLine={{ stroke: "var(--color-border)" }}
             />
             <YAxis
               domain={["auto", "auto"]}
               tickFormatter={(v: number) => `${v}%`}
               tick={{ fontSize: 10, fill: "var(--color-text-tertiary)" }}
               tickLine={false}
               axisLine={false}
               width={40}
             />
             <Tooltip
               formatter={(value: number) => [`${value.toFixed(1)}%`, "전세가율"]}
               labelFormatter={(label: string) => label}
               contentStyle={{
                 background: "var(--color-surface-card)",
                 border: "1px solid var(--color-border)",
                 borderRadius: 8,
                 fontSize: 12,
               }}
             />
             <Line
               dataKey="solidRatio"
               stroke="#3B82F6"
               strokeWidth={2}
               dot={false}
               connectNulls={false}
               name="전세가율"
             />
             <Line
               dataKey="dashedRatio"
               stroke="#3B82F6"
               strokeWidth={2}
               strokeDasharray="5 5"
               dot={false}
               connectNulls={false}
               name="전세가율 (데이터 부족)"
               legendType="none"
             />
           </LineChart>
         </ResponsiveContainer>
         <p className="mt-1 text-[10px]" style={{ color: "var(--color-text-tertiary)" }}>
           * 월별 전세 중위가 / 매매 중위가 기준 · 점선 = 거래 5건 미만
         </p>
       </div>
     );
   }
   ```

5. **Y-axis domain**: Use `["auto", "auto"]` to auto-scale based on actual ratio range, not fixed [0, 100].
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/charts/JeonseRatioChart.tsx exists
    - JeonseRatioChart.tsx contains `"use client"`
    - JeonseRatioChart.tsx contains `import { groupByMonth, computeMedianPrice }` from price-normalization
    - JeonseRatioChart.tsx contains `monthly_rent === 0` filter for pure jeonse
    - JeonseRatioChart.tsx contains `computeMedianPrice(rentPrices) / computeMedianPrice(salePrices) * 100` or equivalent ratio computation
    - JeonseRatioChart.tsx contains `isLowConfidence` check with threshold 5
    - JeonseRatioChart.tsx contains `solidRatio` and `dashedRatio` data keys
    - JeonseRatioChart.tsx contains `strokeDasharray="5 5"` for low-confidence line
    - JeonseRatioChart.tsx contains `height={160}` in ResponsiveContainer
    - JeonseRatioChart.tsx contains `if (chartData.length < 2) return null` or equivalent empty guard
    - Build succeeds
  </acceptance_criteria>
  <done>JeonseRatioChart 컴포넌트가 월별 전세가율을 solid/dashed 듀얼 라인으로 표시. 데이터 5건 미만 월은 점선. 데이터 부족 시 null 반환.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JeonseRatioChart into AptDetailClient</name>
  <files>src/components/apt/AptDetailClient.tsx</files>
  <read_first>
    - src/components/apt/AptDetailClient.tsx (line 259-291 — PriceHistoryChart section, selectedSize state)
    - src/components/charts/JeonseRatioChart.tsx (props interface from Task 1)
    - .planning/phases/11-jeonse-gap-analysis/11-CONTEXT.md (D-10: PriceHistoryChart 아래에 배치)
  </read_first>
  <action>
1. **Import JeonseRatioChart** at the top of AptDetailClient.tsx:
   ```typescript
   import JeonseRatioChart from "@/components/charts/JeonseRatioChart";
   ```

2. **Add filtered txns useMemo** for the selected size's rent transactions (pure jeonse only for chart efficiency, though JeonseRatioChart also filters internally — passing size-filtered data reduces chart re-computation):
   ```typescript
   const selectedSizeRentTxns = useMemo(() => {
     if (!selectedSize) return [];
     return rentTxns.filter((t) => t.size_sqm === selectedSize);
   }, [rentTxns, selectedSize]);

   const selectedSizeSaleTxns = useMemo(() => {
     if (!selectedSize) return [];
     return saleTxns.filter((t) => t.size_sqm === selectedSize);
   }, [saleTxns, selectedSize]);
   ```

3. **Render JeonseRatioChart** after the PriceHistoryChart section (after the closing `</div>` of the `mb-8` div around PriceHistoryChart, approximately after line 291). Per D-10: "기존 PriceHistoryChart 아래에 별도 소형 LineChart 추가":
   ```tsx
   {/* 전세가율 추이 차트 (D-10) */}
   {selectedSize && selectedSizeRentTxns.length > 0 && (
     <div className="mb-8">
       <JeonseRatioChart
         saleTxns={selectedSizeSaleTxns}
         rentTxns={selectedSizeRentTxns}
       />
     </div>
   )}
   ```

4. Per D-14: When `selectedSizeRentTxns.length === 0`, the chart div is not rendered at all. JeonseRatioChart also has its own internal guard (`chartData.length < 2` returns null).
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AptDetailClient.tsx contains `import JeonseRatioChart from "@/components/charts/JeonseRatioChart"`
    - AptDetailClient.tsx contains `selectedSizeRentTxns` useMemo filtering by selectedSize
    - AptDetailClient.tsx contains `selectedSizeSaleTxns` useMemo filtering by selectedSize
    - AptDetailClient.tsx contains `<JeonseRatioChart` render call
    - AptDetailClient.tsx contains `selectedSizeRentTxns.length > 0` guard around chart
    - Build succeeds
  </acceptance_criteria>
  <done>전세가율 추이 차트가 PriceHistoryChart 아래에 표시. 면적 선택 시 해당 면적의 월별 전세가율 추이를 확인 가능. 전세 데이터 없으면 미표시.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. JeonseRatioChart.tsx exists with dual-line pattern
3. AptDetailClient.tsx imports and renders JeonseRatioChart
4. Chart receives size-filtered saleTxns and rentTxns
</verification>

<success_criteria>
- 면적별 전세가율 추이 차트가 PriceHistoryChart 아래에 표시된다
- X축 월별, Y축 전세가율(%) 형태의 LineChart
- 데이터 5건 미만 월은 점선 처리
- 전세 데이터 없는 면적은 차트 미표시
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/11-jeonse-gap-analysis/11-02-SUMMARY.md`
</output>
