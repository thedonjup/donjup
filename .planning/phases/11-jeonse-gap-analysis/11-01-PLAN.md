---
phase: 11-jeonse-gap-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/apt/[region]/[slug]/page.tsx
  - src/components/apt/AptDetailClient.tsx
autonomous: true
requirements: [GAP-01, GAP-02]
must_haves:
  truths:
    - "면적 선택 시 해당 면적의 전세가율(%) 수치가 표시된다"
    - "면적 선택 시 해당 면적의 갭 금액(매매가 - 전세가)이 표시된다"
    - "전세가율 70%+ 빨강, 60-70% 노랑, 60% 미만 초록 색상이 적용된다"
    - "전세 데이터 없는 면적은 '-' 표시된다"
  artifacts:
    - path: "src/components/apt/AptDetailClient.tsx"
      provides: "면적별 전세가율/갭 금액 계산 + 지표 카드 렌더링"
      contains: "jeonseRatio"
    - path: "src/app/apt/[region]/[slug]/page.tsx"
      provides: "Row 2 StatCard(최근 전세가, 전세가율) 제거"
  key_links:
    - from: "AptDetailClient.tsx sizePriceMap"
      to: "GAP 지표 카드 렌더링"
      via: "selectedSize -> sizePriceMap.get(selectedSize)"
      pattern: "sizePriceMap\\.get\\(selectedSize\\)"
---

<objective>
아파트 상세 페이지에 면적별 전세가율(%)과 갭 금액(매매가 - 전세가)을 표시한다.

Purpose: 투자자가 면적 선택 시 갭 리스크를 즉시 파악할 수 있도록 한다.
Output: page.tsx Row 2 StatCard 제거, AptDetailClient에 면적 연동 GAP 지표 카드 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-jeonse-gap-analysis/11-CONTEXT.md
@.planning/phases/11-jeonse-gap-analysis/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/apt/AptDetailClient.tsx:
```typescript
export interface AptTransaction {
  id: string;
  trade_date: string;
  trade_price: number;
  size_sqm: number;
  floor: number;
  highest_price: number | null;
  change_rate: number | null;
  is_new_high: boolean;
  is_significant_drop: boolean;
  deal_type: string | null;
  drop_level?: string;
}

export interface AptRentTransaction {
  id: string;
  size_sqm: number;
  floor: number | null;
  deposit: number;
  monthly_rent: number;
  rent_type: string;
  contract_type: string | null;
  trade_date: string;
}
```

From src/lib/price-normalization.ts:
```typescript
export const LOW_FLOOR_MAX = 3;
export function computeMedianPrice(prices: number[]): number;
export function groupByMonth(txns: { trade_date: string; trade_price: number }[]): { month: string; prices: number[] }[];
```

Current sizePriceMap type (AptDetailClient.tsx line 101):
```typescript
Map<number, { highFloorSale: number | null; lowFloorSale: number | null; latestJeonse: number | null }>
```

page.tsx StatCard function (line 492-513):
```typescript
function StatCard({ label, value, accent }: { label: string; value: string; accent?: "drop" | "rise" }) {
  // ... renders rounded-2xl border p-4 card
}
```

page.tsx Row 2 (lines 390-401):
```html
<div className="grid gap-3 grid-cols-2 mb-8">
  <StatCard label="최근 전세가" value={...} />
  <StatCard label="전세가율" value={...} />
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend sizePriceMap + add GAP indicator cards in AptDetailClient</name>
  <files>src/components/apt/AptDetailClient.tsx</files>
  <read_first>
    - src/components/apt/AptDetailClient.tsx (current sizePriceMap at line 100-114, render at line 207-305)
    - src/lib/price-normalization.ts (LOW_FLOOR_MAX export)
    - .planning/phases/11-jeonse-gap-analysis/11-CONTEXT.md (D-01 through D-09)
  </read_first>
  <action>
1. **Extend sizePriceMap** (line 100-114) to add `latestSale`, `gapAmount`, `jeonseRatio` fields per D-02, D-03, D-04:

   Change the Map type from:
   ```typescript
   Map<number, { highFloorSale: number | null; lowFloorSale: number | null; latestJeonse: number | null }>
   ```
   to:
   ```typescript
   Map<number, {
     highFloorSale: number | null;
     lowFloorSale: number | null;
     latestJeonse: number | null;
     latestSale: number | null;
     gapAmount: number | null;
     jeonseRatio: number | null;
   }>
   ```

2. **Fix jeonseTx filter** (line 106) per D-01 — add `monthly_rent === 0` condition:
   ```typescript
   const jeonseTx = rentTxns.find(
     (t) => t.size_sqm === size && t.rent_type === "전세" && t.monthly_rent === 0
   );
   ```

3. **Add latestSale, gapAmount, jeonseRatio** computations inside the loop:
   ```typescript
   const latestSale = sizeMatches[0]?.trade_price ?? null;
   const latestJeonse = jeonseTx?.deposit ?? null;
   const gapAmount = (latestSale !== null && latestJeonse !== null) ? latestSale - latestJeonse : null;
   const jeonseRatio = (latestSale !== null && latestJeonse !== null && latestSale > 0)
     ? Math.round((latestJeonse / latestSale) * 1000) / 10
     : null;
   ```
   Note: D-02 says use raw trade_price (not Phase 10 adjusted), which `sizeMatches` already provides from `saleTxns`.

4. **Add `getJeonseRatioColor` helper** inside the component (before return):
   ```typescript
   function getJeonseRatioColor(ratio: number | null): string {
     if (ratio === null) return "var(--color-text-primary)";
     if (ratio >= 70) return "var(--color-semantic-drop)";     // red per D-09
     if (ratio >= 60) return "#F59E0B";                         // amber per D-09
     return "var(--color-semantic-rise)";                       // green per D-09
   }
   ```

5. **Add GAP indicator cards** in the JSX, between the size selection card div (ends at line 257 `</div>`) and the PriceHistoryChart div (starts at line 260). Per D-08: "면적 선택 칩 아래, 차트 위":
   ```tsx
   {/* GAP 지표 카드 — 전세가율 / 갭 금액 (D-07, D-08) */}
   {selectedSize && (() => {
     const info = sizePriceMap.get(selectedSize);
     const ratio = info?.jeonseRatio ?? null;
     const gap = info?.gapAmount ?? null;
     const ratioColor = getJeonseRatioColor(ratio);
     return (
       <div className="grid grid-cols-3 gap-3 mb-6">
         <div className="rounded-2xl border p-4" style={{ borderColor: "var(--color-border)", background: "var(--color-surface-card)" }}>
           <p className="text-xs" style={{ color: "var(--color-text-tertiary)" }}>최근 전세가</p>
           <p className="mt-1 text-xl font-extrabold tabular-nums t-text">
             {info?.latestJeonse ? formatPriceShort(info.latestJeonse) : "-"}
           </p>
         </div>
         <div className="rounded-2xl border p-4" style={{ borderColor: "var(--color-border)", background: "var(--color-surface-card)" }}>
           <p className="text-xs" style={{ color: "var(--color-text-tertiary)" }}>전세가율</p>
           <p className="mt-1 text-xl font-extrabold tabular-nums" style={{ color: ratioColor }}>
             {ratio !== null ? `${ratio.toFixed(1)}%` : "-"}
           </p>
         </div>
         <div className="rounded-2xl border p-4" style={{ borderColor: "var(--color-border)", background: "var(--color-surface-card)" }}>
           <p className="text-xs" style={{ color: "var(--color-text-tertiary)" }}>갭 금액</p>
           <p className="mt-1 text-xl font-extrabold tabular-nums t-text">
             {gap !== null ? formatPriceShort(gap) : "-"}
           </p>
         </div>
       </div>
     );
   })()}
   ```

6. Per D-14/D-15: When `latestJeonse` or `latestSale` is null, `jeonseRatio` and `gapAmount` will be null, and the cards will show "-". This is already handled by the ternary logic above.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AptDetailClient.tsx contains `monthly_rent === 0` in the jeonseTx filter
    - AptDetailClient.tsx contains `jeonseRatio` field in sizePriceMap
    - AptDetailClient.tsx contains `gapAmount` field in sizePriceMap
    - AptDetailClient.tsx contains `latestSale` field in sizePriceMap
    - AptDetailClient.tsx contains `getJeonseRatioColor` function
    - AptDetailClient.tsx contains `var(--color-semantic-drop)` in getJeonseRatioColor (70%+ red)
    - AptDetailClient.tsx contains `#F59E0B` (amber for 60-70%)
    - AptDetailClient.tsx contains `var(--color-semantic-rise)` in getJeonseRatioColor (under 60% green)
    - AptDetailClient.tsx contains `grid grid-cols-3 gap-3 mb-6` for GAP cards
    - AptDetailClient.tsx contains text "최근 전세가", "전세가율", "갭 금액"
    - Build succeeds without TypeScript errors
  </acceptance_criteria>
  <done>면적 선택 시 전세가율(%), 갭 금액, 최근 전세가가 3-column 카드에 표시. 전세가율 색상이 위험/주의/양호로 분기. 전세 데이터 없으면 "-" 표시.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Row 2 StatCards from page.tsx</name>
  <files>src/app/apt/[region]/[slug]/page.tsx</files>
  <read_first>
    - src/app/apt/[region]/[slug]/page.tsx (lines 388-404 — Row 2 div with StatCard)
    - .planning/phases/11-jeonse-gap-analysis/11-CONTEXT.md (D-06, D-07)
  </read_first>
  <action>
Per D-06 and D-07: Remove the entire Row 2 div block from page.tsx. This is lines 390-401:

```html
{/* 핵심 지표 카드 - Row 2 */}
<div className="grid gap-3 grid-cols-2 mb-8">
  <StatCard label="최근 전세가" value={latestJeonseDeposit > 0 ? formatPrice(latestJeonseDeposit) : "-"} />
  <StatCard
    label="전세가율"
    value={
      latestPrice > 0 && latestJeonseDeposit > 0
        ? `${((latestJeonseDeposit / latestPrice) * 100).toFixed(1)}%`
        : "-"
    }
  />
</div>
```

Delete this entire block (the `{/* 핵심 지표 카드 - Row 2 */}` comment and the `<div className="grid gap-3 grid-cols-2 mb-8">` with its two StatCard children). The `latestJeonseDeposit` variable may become unused — if so, also remove it and the `latestPrice` variable if no longer referenced elsewhere (check before removing). Keep the `StatCard` function definition (line 492) as it may still be used by Row 1.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx does NOT contain `핵심 지표 카드 - Row 2`
    - page.tsx does NOT contain `grid gap-3 grid-cols-2 mb-8`
    - page.tsx still contains `AptDetailClient` render
    - Build succeeds without errors
  </acceptance_criteria>
  <done>page.tsx의 전체 기준 전세가/전세가율 StatCard가 제거됨. 면적별 연동 카드는 AptDetailClient에서 렌더링.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. AptDetailClient.tsx contains `jeonseRatio`, `gapAmount`, `getJeonseRatioColor`
3. page.tsx no longer contains Row 2 StatCards
4. GAP indicator cards render with 3-column grid
</verification>

<success_criteria>
- 면적 선택 시 전세가율(%), 갭 금액, 최근 전세가가 면적별로 표시된다
- 전세가율 색상이 70%+ 빨강, 60-70% 노랑, 60% 미만 초록으로 적용된다
- page.tsx의 전체 기준 StatCard 2개가 제거되었다
- 전세 데이터 없는 면적은 "-" 표시된다
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/11-jeonse-gap-analysis/11-01-SUMMARY.md`
</output>
