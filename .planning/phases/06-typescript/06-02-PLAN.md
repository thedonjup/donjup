---
phase: 06-typescript
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/page.tsx
  - src/app/rate/page.tsx
  - src/app/daily/[date]/page.tsx
  - src/app/daily/archive/page.tsx
  - src/app/search/page.tsx
  - src/app/compare/page.tsx
  - src/app/market/page.tsx
  - src/app/market/[sido]/page.tsx
  - src/app/market/[sido]/[sigungu]/page.tsx
  - src/components/map/KakaoMap.tsx
  - src/lib/db/client.ts
autonomous: true
requirements:
  - TYPE-03

must_haves:
  truths:
    - "page.tsx의 drops/highs/volume/recent/rates 변수가 concrete 타입을 가진다"
    - "rate/page.tsx의 allRates/bankRatesRaw가 FinanceRate[] 타입을 가진다"
    - "KakaoMap.tsx의 useRef들이 kakao SDK 타입 또는 구체적 타입을 가진다"
    - "핵심 파일의 any 사용이 50% 이상 감소한다"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "타입 적용된 홈페이지"
      contains: "AptTransaction"
    - path: "src/components/map/KakaoMap.tsx"
      provides: "타입 적용된 카카오맵"
  key_links:
    - from: "src/types/db.ts"
      to: "src/app/page.tsx"
      via: "import type"
      pattern: "import.*from.*@/types/db"
    - from: "src/types/db.ts"
      to: "src/app/rate/page.tsx"
      via: "import type"
      pattern: "import.*from.*@/types/db"
---

<objective>
주요 컴포넌트/페이지의 any 타입을 구체 타입으로 교체

Purpose: page.tsx, KakaoMap.tsx 등 핵심 파일에서 any 사용을 제거하여 런타임 에러 가능성을 줄인다.
Output: 핵심 파일의 any 사용 50% 이상 감소, client.ts의 핵심 메서드 타입 강화
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typescript/06-01-SUMMARY.md
@src/types/db.ts
@src/types/api.ts
</context>

<interfaces>
<!-- Types defined in Plan 01 that this plan uses -->
From src/types/db.ts (created in 06-01):
- AptComplex: DB 모델 (region_code, region_name, dong_name, apt_name, slug, ...)
- AptTransaction: DB 모델 (region_code, region_name, apt_name, size_sqm, trade_price, change_rate, is_new_high, drop_level, property_type, ...)
- FinanceRate: DB 모델 (rate_type, rate_value, prev_value, change_bp, base_date, source)
- DailyReport: DB 모델 (report_date, title, summary, top_drops, top_highs, rate_summary, volume_summary, ...)
- PageView: DB 모델 (page_path, page_type, view_count)

From src/components/home/RankingTabs.tsx:
- Transaction: { id, region_code, region_name, apt_name, size_sqm, trade_price, highest_price, change_rate, trade_date, is_new_high?, drop_level?, property_type? }

From src/components/map/map-utils.ts:
- MapTransaction: { id, apt_name, region_name, dong_name, trade_price, change_rate, is_new_high, slug, latitude, longitude, size_sqm, trade_date }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 페이지 컴포넌트 any 제거</name>
  <files>src/app/page.tsx, src/app/rate/page.tsx, src/app/daily/[date]/page.tsx, src/app/daily/archive/page.tsx, src/app/search/page.tsx, src/app/compare/page.tsx, src/app/market/page.tsx, src/app/market/[sido]/page.tsx, src/app/market/[sido]/[sigungu]/page.tsx</files>
  <read_first>src/types/db.ts, src/app/page.tsx, src/app/rate/page.tsx, src/app/daily/[date]/page.tsx, src/app/daily/archive/page.tsx, src/app/search/page.tsx, src/app/compare/page.tsx, src/app/market/page.tsx, src/app/market/[sido]/page.tsx, src/app/market/[sido]/[sigungu]/page.tsx</read_first>
  <action>
    Import types from @/types/db and replace any with concrete types in each file:

    1. src/app/page.tsx:
       - Import AptTransaction, FinanceRate from @/types/db
       - Lines 57-61: `drops: any[]` -> `drops: AptTransaction[]`, same for highs, volume, recent
       - Line 61: `rates: any[]` -> `rates: FinanceRate[]`
       - Line 44: `filterByType(rows: any[], ...)` -> `filterByType<T extends { property_type: number }>(rows: T[], ...): T[]`
       - Line 93: `applyTypeFilter = (q: any)` -> use QueryBuilder type or remove the any (the QueryBuilder from client.ts returns `this`, so the function signature can use a generic or just accept the builder's return type; simplest: type the param as `{ eq: (col: string, val: number) => unknown }`)
       - Remove the `eslint-disable @typescript-eslint/no-explicit-any` comment

    2. src/app/rate/page.tsx:
       - Import FinanceRate from @/types/db
       - Line 40-41: `allRates: any[]` -> `allRates: FinanceRate[]`, same for bankRatesRaw

    3. src/app/daily/[date]/page.tsx:
       - Import DailyReport from @/types/db
       - Line 59: `report: any` -> `report: DailyReport | null`
       - The ReportTransaction, ReportRate, VolumeItem interfaces already exist locally — keep them (they are used for JSONB column casting)

    4. src/app/daily/archive/page.tsx:
       - Import DailyReport from @/types/db
       - Line 53: `(r: any)` -> `(r: DailyReport)`

    5. src/app/search/page.tsx:
       - Line 100: `(d: any)` -> define a SearchResultRow interface locally or import SearchResult from @/types/api
       - Type the mapped result

    6. src/app/compare/page.tsx:
       - Line 74: `rents: any[]` -> define a local Rent interface or use a Pick from AptTransaction
       - Line 109: `catch (e: any)` -> `catch (e: unknown)`, use `e instanceof Error ? e.message : String(e)`

    7. src/app/market/page.tsx:
       - Line 50: `applyTypeFilter = (q: any)` -> same approach as page.tsx

    8. src/app/market/[sido]/page.tsx:
       - Line 61: `topDrop: any; topHigh: any` -> `topDrop: AptTransaction | null; topHigh: AptTransaction | null`

    9. src/app/market/[sido]/[sigungu]/page.tsx:
       - Line 99: `applyTypeFilter = (q: any)` -> same approach as page.tsx

    For the applyTypeFilter pattern that appears in 3 files (page.tsx, market/page.tsx, market/[sido]/[sigungu]/page.tsx), create a shared helper or use a minimal interface: `(q: { eq: (col: string, val: number) => typeof q })`. This avoids importing QueryBuilder (which is a class, not exported).
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup &amp;&amp; npx next build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>grep -c "any" in the modified page files shows at least 50% reduction from baseline (baseline: ~20 any usages across these files)</acceptance_criteria>
  <done>핵심 페이지 파일의 any가 구체 타입으로 교체됨. 빌드 성공.</done>
</task>

<task type="auto">
  <name>Task 2: KakaoMap.tsx + client.ts 타입 강화</name>
  <files>src/components/map/KakaoMap.tsx, src/lib/db/client.ts</files>
  <read_first>src/components/map/KakaoMap.tsx, src/lib/db/client.ts</read_first>
  <action>
    1. src/components/map/KakaoMap.tsx:
       - Kakao SDK has no official TypeScript types. Create a minimal namespace at the top of the file or in a separate src/types/kakao.d.ts:
         ```
         declare namespace kakao.maps {
           class Map { constructor(container: HTMLElement, options: { center: LatLng; level: number }); setCenter(latlng: LatLng): void; getLevel(): number; }
           class LatLng { constructor(lat: number, lng: number); }
           class Marker { constructor(options: { position: LatLng; image?: MarkerImage }); setMap(map: Map | null): void; getPosition(): LatLng; }
           class MarkerImage { constructor(src: string, size: Size, options?: { offset?: Point }); }
           class Size { constructor(width: number, height: number); }
           class Point { constructor(x: number, y: number); }
           class InfoWindow { constructor(options: { content: string; removable?: boolean }); open(map: Map, marker: Marker): void; close(): void; }
           class CustomOverlay { constructor(options: { content: string; position: LatLng; yAnchor?: number }); setMap(map: Map | null): void; }
           namespace event { function addListener(target: unknown, type: string, handler: (...args: unknown[]) => void): void; }
           function load(callback: () => void): void;
         }
         declare namespace kakao.maps.services {
           class MarkerClusterer { constructor(options: { map: kakao.maps.Map; markers?: kakao.maps.Marker[]; gridSize?: number; minLevel?: number; minClusterSize?: number; calculator?: number[]; styles?: object[] }); clear(): void; addMarkers(markers: kakao.maps.Marker[]): void; }
         }
         ```
       - Replace useRef<any>(null) with concrete types:
         - mapInstanceRef: useRef<kakao.maps.Map | null>(null)
         - markersRef: useRef<kakao.maps.Marker[]>([])
         - clustererRef: useRef<kakao.maps.services.MarkerClusterer | null>(null) (or keep a simpler type if clusterer API differs)
         - infoWindowRef: useRef<kakao.maps.InfoWindow | null>(null)
       - Line 16: `kakao: any` in Window -> `kakao: typeof kakao` (referencing the namespace)
       - Line 92: `const markers: any[]` -> `const markers: kakao.maps.Marker[]`

    2. src/lib/db/client.ts:
       - The QueryBuilder class uses `any` extensively for filter values (eq, gt, lt etc.) and in insert/update data. These are intentionally generic since they handle arbitrary SQL values.
       - Replace the most impactful ones:
         - Line 36-37 Condition interface: `values: any[]` -> `values: unknown[]`
         - Lines 57-59 insertData/updateData: `Record<string, any>` -> `Record<string, unknown>`
         - Line 295-297 then(): `(value: any) => any` -> `(value: unknown) => unknown`
         - Line 301-303 execute return: `data: any` -> `data: unknown`
         - Line 710 query return: `rows: any[]` -> `rows: Record<string, unknown>[]`
         - Filter method params (eq, neq, lt, gt, etc.): keep `val: unknown` — changing to specific types would break the Supabase-compatible API
       - Remove the top-level `eslint-disable @typescript-eslint/no-explicit-any` comment
       - After changes, some internal assignments may need type assertions — use `as unknown` rather than `as any` where needed

    WARNING: client.ts changes are high-risk. After each change, run `npx tsc --noEmit` to catch issues immediately. If a specific change causes cascading type errors across the codebase, revert that specific change and keep `any` with a TODO comment explaining why.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/donjup &amp;&amp; npx next build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>grep -c "any" src/components/map/KakaoMap.tsx returns 0 or 1 (Window.kakao may need `any` if namespace approach fails). grep "eslint-disable.*no-explicit-any" src/lib/db/client.ts returns 0.</acceptance_criteria>
  <done>KakaoMap.tsx의 useRef any가 Kakao SDK 타입으로 교체됨. client.ts의 핵심 any가 unknown으로 강화됨. 빌드 성공.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `grep -rn ': any' src/app/page.tsx src/app/rate/page.tsx src/components/map/KakaoMap.tsx` shows 0 or near-0 matches
- `grep -c 'any' src/app/page.tsx` is less than 3 (baseline was ~10)
- `grep -c 'any' src/components/map/KakaoMap.tsx` is less than 2 (baseline was 6)
</verification>

<success_criteria>
- 핵심 페이지/컴포넌트 파일의 any 사용이 50% 이상 감소
- KakaoMap.tsx의 useRef에 구체적 Kakao SDK 타입 적용
- client.ts에서 eslint-disable no-explicit-any 제거
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/06-typescript/06-02-SUMMARY.md`
</output>
